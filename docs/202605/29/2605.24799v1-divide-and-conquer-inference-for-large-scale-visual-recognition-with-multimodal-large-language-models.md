---
title: Divide-and-Conquer Inference for Large-Scale Visual Recognition with Multimodal Large Language Models
title_zh: 面向大规模视觉识别的多模态大语言模型分治推理
authors: "Zhipeng Ye, Jiaqi Huang, Feng Jiang, Qiufeng Wang, Yikang Duan, Dawei Wang, Xihang Zhou, Qian Qiao"
date: 2026-05-24
pdf: "https://arxiv.org/pdf/2605.24799v1"
tags: ["query:multi-modal"]
score: 9.0
evidence: 提出分治推理方法解决多模态大模型在大规模视觉识别中的性能退化问题
tldr: 多模态大语言模型在大规模图像分类中因长序列信息熵与注意力稀释冲突导致性能崩溃。提出分治推理DCI，递归分解分类任务为局部子问题并动态剪枝压缩搜索空间，提升信噪比与精度。在ImageNet-1K/21K上显著提升准确率，使轻量开源模型无需训练即可媲美闭源模型。该模型无关即插即用范式为大规模场景下MLLMs推理精度的高效扩展提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.24799v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1132, \"height\": 841, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24799v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1240, \"height\": 684, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24799v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1201, \"height\": 836, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24799v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1244, \"height\": 549, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.24799v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1258, \"height\": 692, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.24799v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1263, \"height\": 1225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24799v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1274, \"height\": 730, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24799v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1263, \"height\": 241, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24799v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1257, \"height\": 496, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24799v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1266, \"height\": 790, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.24799v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1267, \"height\": 318, \"label\": \"Table\"}]"
motivation: 多模态大语言模型在长序列识别中性能崩溃，源于信息熵升高与注意力机制稀释、退化的根本冲突。
method: 提出分治推理DCI，递归分解全局分类任务为局部子问题，并采用动态剪枝机制压缩搜索空间，优化局部信噪比。
result: 在ImageNet-1K和ImageNet-21K上持续提升分类精度，轻量开源模型无需额外训练即可超越前沿闭源模型。
conclusion: DCI作为模型无关的即插即用范式，以更优的计算缩放行为高效扩展了多模态大语言模型在大规模场景下的推理精度。
---

## 摘要
多模态大语言模型（MLLMs）在广泛的视觉语言任务中展现了强大的能力。然而，当应用于大规模图像分类时，随着标签空间的扩大，其性能显著下降——我们将这一现象定义为长序列识别中的性能崩溃。通过信息论分析，我们揭示这种崩溃源于不断攀升的信息熵与注意力机制中显著的注意力稀释与衰减之间的根本冲突，这损害了模型在处理极长提示时维持足够信噪比的能力。为缓解这一问题，我们提出了分治推理（DCI），一种针对MLLMs视觉识别的新型测试时扩展策略。DCI递归地将复杂的全局分类任务分解为多个更简单的局部子问题，并采用动态剪枝机制压缩搜索空间。该方法通过缓解长序列推理中固有的权重稀释问题，有效提升了局部信噪比和模型准确性。此外，传统自注意力机制面临平方级计算复杂度的严峻挑战，而DCI在大规模分类场景中实现了更优的扩展行为并显著加速推理。在ImageNet-1K和ImageNet-21K等基准上的大量实验表明，DCI持续提升了分类精度。这使得轻量级开源模型无需额外训练或微调即可与前沿闭源巨头相媲美甚至超越。作为一种模型无关、即插即用的范式，DCI为在大规模场景中扩展MLLMs的推理精度提供了一种高效途径。

## Abstract
Multimodal Large Language Models (MLLMs) have demonstrated strong capabilities across a wide range of vision language tasks. However, when applied to large scale image classification, their performance degrades significantly as the label space expands a phenomenon we define as Performance Collapse in Long Sequence Recognition. Through an information theoretic analysis, we reveal that this collapse stems from a fundamental conflict between the escalating information entropy and the prominent attention dilution and decay within attention mechanisms, which impairs the model's ability to maintain a sufficient signal-to-noise ratio when processing extremely long prompts. To mitigate this, we propose Divide-and-Conquer Inference (DCI), a novel test-time scaling strategy for visual recognition with MLLMs. DCI recursively decomposes complex global classification tasks into multiple simpler, localized subproblems and employs a dynamic pruning mechanism to compress the search space. This method effectively improves the local signal to noise ratio and model accuracy by mitigating the inherent weight dilution issues in long-sequence inference. Moreover, while traditional self-attention incurs a prohibitive quadratic computational complexity, DCI achieves more favorable scaling behavior and substantially accelerates inference in large scale classification scenarios. Extensive experiments on benchmarks such as ImageNet-1K and ImageNet-21K demonstrate that DCI consistently improves classification accuracy. This enables lightweight open-source models to rival or even surpass frontier closed-source giants without any additional training or fine-tuning. As a model-agnostic, plug-and-play paradigm, DCI offers an efficient approach for scaling the inferential precision of MLLMs in large-scale scenarios.

---

## 论文详细总结（自动生成）

好的，作为一名资深学术论文分析助手，我将根据您提供的论文内容和要求，生成本文的详细中文总结。

# 论文总结：面向大规模视觉识别的多模态大语言模型分治推理（DCI）

## 1. 核心问题与研究动机

- **核心问题**：多模态大语言模型（MLLMs）在大规模图像分类任务中，随着候选类别（标签空间）的扩大，分类精度会急剧下降。论文将此现象定义为“长序列识别中的性能崩溃”。
- **传统认知**：普遍观点认为这是由于缺乏领域特定训练数据所致。
- **本文发现**：作者通过实验证明，性能瓶颈主要源于**标签空间本身的增大**。无论模型架构或规模如何，当候选类别从少量（如20类）增加到大量（如1000类）时，准确率都会发生灾难性下降。

## 2. 论文提出的方法论

### 2.1 核心思想
提出**分治推理（Divide-and-Conquer Inference, DCI）** 框架。这是一种测试时扩展策略，无需额外训练。其核心思想是将一个复杂的、包含大量候选类别的全局分类任务，递归地分解成一系列更简单的、并行的局部子问题，通过逐步缩小搜索空间来完成最终分类。

### 2.2 关键技术细节
DCI包含三个核心阶段，并循环执行直至终止：
1.  **Divide（分解）**：将当前的候选类别集 `Ct` 均匀划分为多个大小不超过 `K` 的子集 `C(i)t`。其中 `K` 是控制分组大小的超参数。
2.  **Conquer（攻克）**：对每个子集 `C(i)t`，使用一个结构化的提示模板（含“None”输出选项）让MLLM并行进行独立预测。若子集中不含正确类别，模型应输出“None”。
3.  **Combine（合并与剪枝）**：收集所有子问题的输出 `At`，通过“分支剪枝”过滤掉结果为“None”的无效分支，保留有效的预测类别作为下一轮的候选集 `Ct+1`。
4.  **递归与终止**：以新的候选集 `Ct+1` 作为输入，重复以上过程。终止条件为：
    - 候选集为空，输出“None”（未识别出）。
    - 候选集只剩一个类别，直接输出。
    - 候选集大小 ≤ `K`，直接使用MLLM进行最终预测。

### 2.3 核心公式与理论
- **信息赤字定理（Theorem 1）**：模型对大规模分类的错误率 `Pe` 存在一个理论下界，该下界随候选集大小 `K` 的增大而趋近于1。公式表明标准扁平推理（一次性对所有类别进行推理）存在固有限制。
- **计算复杂度分析**：标准扁平推理复杂度为 `O(N^2)`。DCI的复杂度约为 `(N/K + log_K N - 1) * K^2`。在超大类别（如N=21k）时，DCI的复杂度远低于扁平推理，形成 **“DCI优势区域”**，实现了精度和速度的双赢。

## 3. 实验设计

- **数据集与Benchmark**：
    - **中等规模**：CIFAR-100, CUB-200, Food-101。
    - **大规模**：ImageNet-1K (1000类), ImageNet-21K (21,000类)。
- **对比方法**：
    - **Baseline**：无任何策略的扁平推理。
    - **其他测试时扩展方法**：CoT (Chain-of-Thought)， SC (Self-Consistency)。
    - **不同策略消融**：随机分组、最相似分组、最不相似分组。
- **评估模型**：
    - **开源模型**：Gemma3-4B, GLM-4V-9B, LLaMA-3.2-11B, DeepSeek-VL-7B, Qwen2.5-VL-7B, Qwen3-VL系列(2B/4B/8B)。
    - **闭源模型**：GPT-4(v4.1), Claude Opus 4.5, Kimi-K2.5, Qwen3-VL-Plus。
- **主要对比角度**：
    1.  **有效性**：在多个数据集上比较有无DCI的绝对准确率。
    2.  **可扩展性**：在ImageNet-21K上的精度提升。
    3.  **效率**：在ImageNet-21K上的推理延迟对比。
    4.  **消融实验**：不同分组大小 `K` 和不同分组策略的影响。
    5.  **泛化性**：在多个模型家族和规模上的表现。

## 4. 资源与算力

- **硬件配置**：论文明确提及了硬件配置：服务器配备AMD EPYC 7642处理器、四块NVIDIA RTX 4090显卡、256GB DDR4内存和2TB/8TB固态/机械硬盘。
- **软件环境**：Ubuntu 22.04.3 LTS, Python 3.10, PyTorch 2.8.0, Hugging Face Transformers 4.57.3, vLLM 0.15.0。
- **关于训练**：论文特别指出DCI是**训练自由**的方法，因此**没有**提及模型训练所需的GPU小时数等资源消耗。其资源消耗主要体现在推理阶段，实验报告了推理延迟。
- **备注**：论文明确声明，由于DCI在推理时会进行多轮交互，考虑到闭源API费用，对闭源模型只采用了5000张图片的子集进行测试，而开源模型使用了50000张图片。

## 5. 实验数量与充分性

- **实验数量**：非常充分。涵盖了6个不同数据集、8种以上不同的MLLM模型（包括开源和闭源、不同规模）、以及与2种主流测试时扩展方法的对比。
- **充分性与公平性**：
    - **充分**：实验覆盖了从100类到21,000类的广泛场景，验证了方法的有效性与可扩展性。对关键超参数 `K` 做了细致的消融分析（从2到5000），并探讨了不同的分组策略。
    - **公平**：对比方法（CoT, SC）在相同的模型和数据集上测试。在对比基线时，统一了MLLM的配置（精度、采样策略）。对闭源和开源模型采用不同的测试集规模（5000 vs 50000）也体现了对计算成本的合理考量。

## 6. 主要结论与发现

1.  **验证了PC-LSR现象**：信息论分析证实，长序列性能崩溃是MLLM在大规模分类中的固有问题，源于信息熵需求与自注意力机制信号稀释之间的矛盾。
2.  **DCI显著有效**：DCI在所有实验中均一致地提升了分类准确率。例如，在ImageNet-21K上，Qwen2.5-VL-7B的准确率从0.21%提升至37.87%，提升了近180倍。
3.  **轻量模型可媲美闭源模型**：轻量级开源模型（如Qwen3-VL-8B）结合DCI后，其平均准确率（71.32%）超越了强大的闭源模型GPT-4 (v4.1)（70.43%）。
4.  **精度与效率的“双赢”**：在大规模场景下（如ImageNet-21K），选择恰当的 `K` 值（如K=500），DCI不仅在精度上远超扁平推理，在推理速度上也实现了**加速**。这打破了精度和速度通常需要权衡的传统认知。
5.  **模型无关性**：DCI的增益在不同模型家族（Qwen, LLaMA, Gemma等）和模型规模（2B, 4B, 8B, 11B）上均能观察到，证明其是一种通用的、无模型依赖的增强手段。

## 7. 优点

1.  **创新性高**：首次系统地揭示了MLLM在大规模分类中的性能崩溃现象，并给出了严谨的信息论解释。提出的DCI是解决该问题的一个优雅且有效的策略。
2.  **实用性极强**：无需训练、即插即用、模型无关，使其对学术界和工业界都有极高的吸引力。只需改变推理策略即可显著提升性能。
3.  **理论扎实**：不仅提出了方法，还通过信息熵和注意力稀释理论（信息赤字定理）给出了坚实的基础，解释了方法有效的原因。
4.  **效果显著且反直觉**：在超大类别场景下，同时提升了**准确率和推理速度**，这是非常有价值且反直觉的突破，证明了结构化推理的价值。
5.  **实验非常全面**：涵盖了不同规模的数据集、模型和SOTA方法，结果具有很高的可信度和说服力。

## 8. 不足与局限

1.  **小规模场景下的效率问题**：在类别规模较小的任务中（如CIFAR-100, ImageNet-1K），DCI会引入额外的推理延迟（如文中表4所示，latency从0.2s到0.37s）。虽然精度有提升，但牺牲了速度。
2.  **对基础模型能力的要求**：DCI的有效性依赖于MLLM在“短序列”子问题上的可靠判断能力。如果基础模型本身在短序列识别上就存在问题，DCI的效果可能会受限（尽管实验证明它依然能给最差的模型带来提升）。
3.  **隐含的标签空间假设**：方法假设类别是互斥的，且能有效输出“None”。对于某些高度细粒度或模糊的图像，模型输出“None”或错误类别可能导致分支剪枝失败，从而引入错误累积。
4.  **并行化开销**：虽然理论上可以并行执行Conquer阶段，但实际中并行化需要额外的调度和通信开销，文中未深入探讨这些实际系统层面的挑战。
5.  **实验覆盖的偏差**：文中提到对大模型的闭源服务因成本原因只测试了5000张图片，这可能引入一定的采样偏差，虽然合情合理，但仍是实验的一个限制。

（完）
