---
title: "LoMo: Local Modality Substitution for Deeper Vision-Language Fusion"
title_zh: "LoMo: 局部模态替换以实现更深层视觉-语言融合"
authors: "Feng Han, Zhixiong Zhang, Zheming Liang, Yibin Wang, Jiaqi Wang"
date: 2026-05-28
pdf: "https://arxiv.org/pdf/2605.30265v1"
tags: ["query:multi-modal"]
score: 9.0
evidence: 视觉语言模型融合方法
tldr: 视觉语言模型(VLM)在文本问题替换为对应图像后性能大幅下降，源于训练数据中文本与图像角色不对称导致跨模态表示不对齐。提出Local Modality Substitution(LoMo)，一种轻量级数据整理方法，通过将单模态提示重构为文本-图像-文本交错序列，动态替换文本片段为渲染图像，提供跨模态不变性监督。在13个多模态基准上，LoMo显著提升推理能力，在LLaVA-OneVision和Qwen3.5上分别提升2.67和2.82个点。该方法有效缓解载体敏感性问题，实现更深层的跨模态融合。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30265v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1438, \"height\": 382, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30265v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1441, \"height\": 500, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30265v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1422, \"height\": 531, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30265v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1459, \"height\": 648, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.30265v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1453, \"height\": 502, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30265v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1454, \"height\": 229, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30265v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1454, \"height\": 250, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30265v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1449, \"height\": 193, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30265v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 736, \"height\": 703, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30265v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1169, \"height\": 376, \"label\": \"Table\"}]"
motivation: 训练数据中文本与图像角色不对称导致VLM对载体敏感，无法对齐语义等价的跨模态表示。
method: LoMo将单模态提示重构为文本-图像-文本交错序列，动态选择文本片段替换为渲染图像，监督跨模态表示不变性。
result: 在13个多模态基准上，LoMo显著提升推理与融合，在LLaVA-OneVision-1.5-8B和Qwen3.5-9B上分别比标准SFT提高2.67和2.82点。
conclusion: LoMo轻量通用，有效解决载体敏感问题，推动更深的视觉-语言融合。
---

## 摘要
视觉-语言模型（VLM）通过大规模图像-文本训练实现多模态融合，在许多理解和推理任务上取得了显著进展。理想情况下，将文本问题替换为其渲染图像对应物应基本不影响模型性能。然而，实践中这种模态替换会导致严重的性能下降。我们将这种“载体敏感性”问题归因于当前训练语料中固有的偏差。在图像描述、VQA、OCR和网络来源的交错数据等常见数据集中，文本和图像通常被组织成不同且不对称的角色，文本作为语言查询，图像作为视觉参考。这种数据偏差导致VLM对不同模态的信息获取表现出不同的偏好。因此，VLM无法对齐语义等价内容在文本和视觉载体上的表示，使得模型推理在模态替换下变得脆弱。为了解决这个问题，我们提出了局部模态替换（LoMo），一种轻量级、与架构无关的数据整理范式，旨在为语义等价的文本和图像载体之间的跨模态表示不变性提供监督。LoMo通过将单模态提示重构为无缝交错的多模态序列来实现这一点。它动态选择目标文本跨度并将其重新表述为渲染图像，从而在“文本、视觉、文本”载体上保持相同的语义。在13个不同的多模态基准测试上的大量实验表明，LoMo显著改善了整体多模态推理，并实现了更深层的跨模态融合。具体来说，它在基础模型上带来了一致的改进，在LLaVA-OneVision-1.5-8B上比标准SFT提高了2.67个百分点，在Qwen3.5-9B上提高了2.82个百分点。

## Abstract
Vision-Language Models (VLMs) have achieved substantial progress across a wide range of understanding and reasoning tasks, driven by large-scale image-text training aimed at multimodal fusion. Ideally, replacing a textual question with its rendered-image counterpart should leave model performance essentially unaffected. In practice, however, such modality substitution induces dramatic performance degradation. We attribute this "carrier sensitivity" issue to an inherent bias in current training corpora. Across prevalent datasets such as image captioning, VQA, OCR, and web-sourced interleaved data, text and images are typically organized into distinct and asymmetric roles, with text serving as linguistic queries and images as visual references. Such data bias leads VLMs to exhibit distinct preferences for information acquisition across different modalities. Consequently, VLMs fail to align representations of semantically equivalent content across textual and visual carriers, making model reasoning fragile under modality substitution. To address this, we propose Local Modality Substitution (LoMo), a lightweight, architecture-agnostic data curation paradigm designed to provide supervision for cross-modal representational invariance between semantically equivalent text and image carriers. LoMo achieves this by reformulating single-modality prompts into seamlessly interleaved multimodal sequences. It dynamically selects target text spans and recasts them as rendered images, thereby preserving the same semantics across "text, visual, text" carriers. Extensive experiments across 13 diverse multimodal benchmarks demonstrate that LoMo significantly improves overall multimodal reasoning and yields deeper cross-modal fusion. Specifically, it delivers consistent gains across foundational models, improving over standard SFT by 2.67 points on LLaVA-OneVision-1.5-8B and 2.82 points on Qwen3.5-9B.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **现象**：当前先进的视觉-语言模型（VLM）存在严重的“载体敏感性”（carrier sensitivity）问题——将多模态查询中的文本问题替换为语义完全相同的渲染图像后，模型性能出现一致且显著的下降（图1(a)）。
- **根源**：作者归因于训练语料中文本与图像角色不对称的固有偏差。在caption、VQA、OCR等数据集中，文本通常作为语言查询/指令，图像作为视觉参考/证据，导致VLM对不同模态的信息获取产生不同偏好，进而无法对齐语义等价的跨模态表示。
- **重要性**：该问题暴露了当前VLM跨模态融合的脆弱性，限制了模型对真实世界中混合视觉-文本信息的鲁棒处理能力。

## 2. 论文提出的方法论

### 核心思想
通过**局部模态替换**（Local Modality Substitution, LoMo），将文本-only样本重构为“文本→图像→文本”交错序列，为标准SFT隐式地引入跨模态对齐监督，促使模型学习语义等价内容在不同载体上的表示不变性。

### 关键技术细节（三个连续阶段）

1. **结构感知跨度定位（Structure-Aware Span Localization）**
   - 估计输入长度（按句子数），短文本整体作为替换目标；长文本进行数学公式感知的分块，将公式视为原子单元。
   - 提取序列中间三分之一的内容块作为替换目标 \( x_{mid} \)，保留前后文本 \( x_{pre} \) 和 \( x_{suf} \)，形成“文本→视觉→文本”骨架。

2. **视觉渲染（Visual Rendering）**
   - 内容感知路由：含数学表达式的跨度使用LaTeX渲染器，其他使用通用文本渲染器。
   - 降级回退机制：LaTeX失败时自动回退到文本渲染，避免丢弃样本。
   - 轻量边缘裁剪，控制图像大小不改变语义。

3. **感知失真（Perceptual Distortion）**
   - 施加四种保持语义的退化操作（可选择一项或保持不变）：
     - 旋转（Rotate）
     - 模糊（Gaussian/box/motion blur）
     - 阴影/污渍（Shadow-or-stain）
     - 波浪变形（Wave）
   - 模拟真实文档捕获中的噪声，增强鲁棒性。

### 公式与算法流程（文字说明）
- 原始样本 \((x, a)\) 经 \(S(\cdot)\) 得到三部分，再经 \(R(\cdot)\) 渲染为图像 \(I\)，最后经 \(A(\cdot)\) 失真得到 \(I'\)，最终输入为 \((x_{pre}, I', x_{suf})\)，监督目标仍为 \(a\)。
- 优化目标分析：将LoMo的损失函数 \(\mathcal{L}_{LoMo} = -\log p_\theta(a|T(x))\) 展开为标准SFT项 + KL散度项（驱动模型在文本与视觉载体上的预测分布趋于一致），从而获得隐式跨模态对齐。

## 3. 实验设计

- **模型**：LLaVA-OneVision-1.5-8B-Base 和 Qwen3.5-9B-Base（架构差异显著）。
- **训练数据**：从官方LLaVA-OneVision-1.5 SFT语料中随机采样200万多模态 + 200万纯文本指令样本。标准SFT直接在此池上微调；LoMo将其中50%的纯文本样本改造成交错序列。
- **评估基准（13个）**，涵盖6大类别：
  - 通用推理：MMMU, MMMU-Pro
  - 数学推理：MathVista, ZeroBench, WeMath
  - 事实性：SimpleVQA, HallusionBench
  - 指令跟随：MM-IFEval
  - 文档与OCR：MMLongBench-Doc, DocVQA, CC-OCR
  - 视觉感知：V*, CountBench
- **对比方法**：主要对比标准SFT（同数据规模、优化器、学习率、步数），同时对比了全文本渲染变体、无感知失真变体、不同替换比例/位置等。
- **评估协议**：标准评估（原始多模态输入）和渲染评估（将文本问题全渲染为图像），采用EvalScope统一设置。

## 4. 资源与算力

- **数据构建**：两个CPU服务器（各128核），耗时约20小时。
- **训练与评估**：单节点8块NVIDIA H200 GPU。
- **训练超参**：最大序列长度32,768 tokens，最大图像分辨率2,560,000像素，bf16精度，DeepSpeed ZeRO Stage 1，AdamW优化器，学习率余弦调度4e-5→1e-6，warmup 0.002，单卡batch size 1（梯度累积4步），训练1个epoch。
- **注意**：论文未明确给出单次训练的具体GPU时长，但描述了上述资源配置。

## 5. 实验数量与充分性

- **主实验**：两个模型 × 13个基准 × 2种评估协议 = 52个结果（表1），LoMo在26个对比中23个提升。
- **交叉模态表示分析**：采用MIR（模态集成率）和成对交叉模态距离两个指标，在不同数据规模（1M/2M/3M/4M）下跟踪变化（图4）。
- **消融实验**：
  - 组件消融（表2）：全文本渲染、无感知失真、完整LoMo。
  - 数据规模消融（图4）：4种规模。
  - 重写比例消融（表3）：0%/25%/50%/75%/100%。
  - 渲染位置消融（表4）：前缀/中间/后缀/多跨度。
  - 控制对比（表5）：匹配图像-文本比例1:1 vs LoMo天然3:1，排除更多视觉样本的干扰。
- **纯文本能力检查**（附录A）：5个纯文本基准（MMLU-Pro, GSM8K, HumanEval, LiveCodeBench, IFEval），验证LoMo不损害语言能力。
- **充分性评价**：实验覆盖广泛，消融全面（组件、数据量、比例、位置、控制变量），且客观比较了同一基线。使用了权威基准和统一评估框架，公平性较好。但限于两个模型（8B-9B）和SFT阶段，未在预训练或更大模型上验证，存在一定局限性。

## 6. 论文的主要结论与发现

1. **载体敏感性普遍存在**：4种主流VLM均出现显著性能下降（5.70%~11.68%），且下降幅度与文本-渲染图像表示距离正相关。
2. **LoMo有效缓解载体敏感性**：
   - 在标准评估下，LLaVA-OV1.5-8B平均提升2.68点，Qwen3.5-9B提升2.82点（表1）。
   - 在渲染评估下提升更显著（+18.86和+11.92），将标准→渲染的下降压缩至2.07点（Qwen3.5-9B）。
3. **跨模态对齐增强**：MIR降低0.122，成对跨模态距离从0.57降至0.49（4M规模），而标准SFT反而使距离上升（图4）。
4. **来源分析**：LoMo的关键在于结构化中间跨度替换和感知失真，全文本渲染增益有限，50%重写比例最佳，中间位置最好，收益不来自简单增加视觉样本数（表5）。
5. **纯文本能力保持**：纯文本基准上平均提升0.28/0.58点，未见下降。

## 7. 优点

- **轻量通用**：无需修改模型架构、无需额外标注、零推理开销，可即插即用于任何多模态训练流程。
- **数据驱动**：从训练数据源头解决模态对齐问题，思路新颖，与现有架构和训练方式正交。
- **理论支撑**：将LoMo损失函数展开为KL散度项，揭示了隐式跨模态对齐的数学本质。
- **实验严谨**：大量消融和控制实验，排除混淆因素（如更多图像样本）的干扰，验证了核心设计贡献。
- **效果显著**：在13个基准上一致提升，尤其对感知和指令跟随任务提升明显，且不损害纯文本能力。
- **无偏见风险**：方法本身不引入新偏差，仅增强对语义载体的鲁棒性。

## 8. 不足与局限

- **实验范围受限**：仅验证了SFT阶段，未见预训练或RL后训练的结合；仅测试了两个8B-9B模型，更大规模（如70B+）的泛化性未知。
- **跨度定位策略简单**：采用固定的中间三分之一启发式，可能并非最优；作者也提到了可探索难度感知或课程式选择。
- **渲染质量依赖**：LaTeX渲染失败时回退到文本渲染，数学公式的渲染保真度可能影响对齐效果。
- **推理开销忽略**：虽然LoMo本身无推理开销，但渲染评估协议本身仍为研究设定，实际部署中未必需要模拟该场景。
- **数据集偏差**：训练数据仅来自LLaVA-OneVision-1.5 SFT语料，可能在其他数据分布上表现不同。
- **安全性考量**：论文提及LoMo继承预训练模型的偏见和安全问题，建议部署时遵循已有安全实践，但未提供额外缓解措施。

（完）
