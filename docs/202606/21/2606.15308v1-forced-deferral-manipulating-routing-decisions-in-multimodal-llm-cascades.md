---
title: "Forced Deferral: Manipulating Routing Decisions in Multimodal LLM Cascades"
title_zh: 强制延迟：操纵多模态大模型级联中的路由决策
authors: "Zhongye Liu, Yaopei Zeng, Yurui Chang, Lu Lin"
date: 2026-06-13
pdf: "https://arxiv.org/pdf/2606.15308v1"
tags: ["query:multi-modal"]
score: 9.0
evidence: 攻击多模态LLM级联系统，属于多模态语言模型核心研究
tldr: 多模态大语言模型级联系统利用弱模型置信度控制是否调用强模型，以节省计算成本。本文揭示此机制易受攻击，提出强制推迟攻击（FDA），通过优化温度平坦化目标学习通用边界触发器，降低弱模型在触发输入上的置信度，迫使查询被路由到强模型。实验表明，FDA在多个数据集和模型上显著增加强模型调用率，优于现有基线。该工作首次系统性地暴露了级联系统在计算分配层面的安全漏洞。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.15308v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1377, \"height\": 595, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15308v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1299, \"height\": 698, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15308v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1302, \"height\": 320, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15308v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 691, \"height\": 688, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15308v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1143, \"height\": 422, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15308v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1303, \"height\": 340, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15308v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1442, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15308v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1440, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15308v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1440, \"height\": 338, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15308v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1439, \"height\": 340, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.15308v1/fig-011.webp\", \"caption\": \"\", \"page\": 0, \"index\": 11, \"width\": 1441, \"height\": 338, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.15308v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 813, \"height\": 348, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15308v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 801, \"height\": 247, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15308v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1445, \"height\": 294, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15308v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 996, \"height\": 207, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.15308v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1461, \"height\": 205, \"label\": \"Table\"}]"
motivation: 弱模型置信度直接控制级联系统的计算分配，攻击者可操纵该信号强制路由到强模型，造成资源滥用。
method: FDA通过温度平坦化目标优化通用边界触发器，使弱模型在触发输入上的token分布偏离其原始正确输出。
result: 在六个数据集、多种模型组合下，FDA均显著提升强模型调用比例，效果优于FGSM等基线方法。
conclusion: MLLM级联系统存在计算分配操纵风险，需要开发针对性的防御策略来防止强制延期攻击。
---

## 摘要
尽管多模态大语言模型（MLLM）展现了强大的视觉推理能力，但为每个查询都服务一个大型模型的计算成本高昂。MLLM级联通过首先查询一个较弱但成本较低的模型，并在弱模型输出不自信时交由强模型处理来降低这一成本。然而，由于弱模型的置信度直接控制计算分配，这些系统暴露了一个新的攻击面：攻击者可以操纵置信度，使其查询被持续地交由强模型处理。受这一漏洞的启发，我们提出了强制延迟攻击（FDA），这是一种对抗性图像攻击，通过降低弱模型的置信度，使级联系统将查询路由到强模型。FDA通过优化一个温度平滑的目标函数来学习一种通用的边界触发器。该目标函数将弱模型在触发输入上的令牌分布推向根据其干净响应构建的、更不集中的目标。在多个数据集、模型家族和延迟指标上，FDA持续增加了强模型路由的比例，同时优于图像扰动和提示注入基线。这些结果表明，MLLM级联容易受到操纵计算分配的攻击，从而在不直接针对答案正确性的情况下强制使用非预期的强模型。

## Abstract
While multimodal large language models (MLLMs) have shown strong visual reasoning abilities, serving a large model for every query is computationally expensive. MLLM cascades mitigate this cost by first querying a weak but cheaper model and deferring to a strong model when the weak model's output is unconfident. However, since the weak model's confidence directly controls compute allocation, these systems expose a new attack surface: an adversary can manipulate confidence so that their queries are consistently deferred to the strong model. Motivated by this vulnerability, we introduce the Forced Deferral Attack (FDA), an adversarial image attack that lowers the weak model's confidence and causes cascades to route queries to the strong model. FDA learns a universal border trigger by optimizing a temperature-flattened objective. This objective pushes the weak model's token distribution on triggered inputs toward less concentrated targets constructed from its clean responses. Across datasets, model families, and deferral metrics, FDA consistently increases strong-model routing while outperforming image-perturbation and prompt-injection baselines. These results show that MLLM cascades are vulnerable to attacks that manipulate compute allocation, forcing unintended strong-model usage without directly targeting answer correctness.

---

## 论文详细总结（自动生成）

## 论文结构化总结

### 1. 核心问题与整体含义（研究动机和背景）

- **背景**：多模态大语言模型（MLLM）在视觉问答等任务中表现出色，但为每个查询都部署大型模型推理成本高昂。为平衡成本与质量，业界常采用级联系统：先由一个便宜但能力较弱的模型（weak model）处理查询，只有当弱模型输出不自信（低置信度）时才将查询路由到更强大的模型（strong model）。
- **核心问题**：弱模型的置信度信号直接控制计算分配，这暴露了一个新的攻击面——攻击者可以操纵弱模型的置信度，使其**故意降低**，从而强迫级联系统将大量查询路由到强模型，获取更高质量回答的同时将额外计算成本转嫁给服务提供方。现有对抗攻击多关注输出准确性或安全对齐，忽略了计算分配层面的脆弱性。
- **整体含义**：本文首次系统性地揭示了MLLM级联系统在计算分配上的安全漏洞，提出了**强制延迟攻击（Forced Deferral Attack, FDA）**，表明这类攻击不仅有效，而且能在保护输入语义的同时强制重路由。

### 2. 方法论：核心思想与关键技术细节

- **核心思想**：学习一个**通用的、可迁移的边界触发器（universal border trigger）**，将其添加到图像上，使得弱模型对该触发图像的token输出分布变得**更不集中（更平坦）**，从而降低所有常见置信度指标（如最大软概率、困惑度、Jaccard相似度）的数值，达到强制路由到强模型的目的。
- **关键技术细节**：
    1. **目标函数设计（温度平坦化KL散度）**：
        - 对于每个训练样本，先获取弱模型在干净图像上的logits，然后用温度参数 \(T>1\) 的softmax将其缩放为**平坦目标分布** \(q^T_t = \text{softmax}(z_t / T)\)。该目标保留了原始token排序但降低了集中度。
        - 在触发图像上，利用教师强制（teacher forcing）优化，使弱模型的预测分布 \(p_w\) 逼近该平坦目标分布，即最小化KL散度：
          \[
          \delta^* = \arg\min_\delta \frac{1}{|D|}\sum_{(x_I,x_Q)}\frac{1}{L}\sum_{t=1}^L D_{KL}(q^T_t \| p_w(\cdot|(\tilde x_I(\delta),x_Q), y_{<t}))
          \]
        - 该目标**度量无关**（metric-agnostic），因为所有常见置信度指标都是分布集中度的函数，平坦化分布即可同时降低多种指标。
    2. **触发器参数化（通用边界触发器）**：
        - 触发器仅占据图像的**边框区域**（如宽度16像素），中心区域保留原始图像内容，从而**保持语义**，确保路由到强模型后仍能正确回答。
        - 通过二进制掩码 \(b\) 实现：\(\tilde x_I(\delta) = b \odot \delta + (1-b) \odot r(x_I)\)。其中 \(r(x_I)\) 是将原图缩放到中心区域。
    3. **通用性**：触发器在整个训练集上优化一次，即可直接应用于任意新查询（无需每次重新优化）。
- **算法流程**（文字说明）：
    - 训练阶段：在训练集上，对每个样本记录弱模型在干净输入下的生成轨迹（logits），计算温度平坦目标；然后对所有样本，通过梯度下降优化触发器的像素值（边框部分），使弱模型在添加触发器后的图像上的token分布匹配平坦目标。
    - 推理阶段：将学到的触发器直接叠加到新查询图像的边框上，输入弱模型。由于置信度被压低，级联系统会将大部分查询路由到强模型。

### 3. 实验设计

- **数据集**：MMBench、ScienceQA、MMMU。均为图像多选题，可自动评估答案正确性。
- **Benchmark与对比方法**：
    - **级联系统**：两个同族级联（Gemma4-E2B→Gemma4-26B；Qwen3-VL-2B→Qwen3-VL-32B）及交叉家族级联（弱模型用Gemma，强模型用Qwen等）。
    - **对比基线**：
        - 去模糊（Defocus blur）：一种通用图像扰动。
        - 提示注入（Prompt injection）：修改文本查询让模型自我质疑。
        - 清洁级联（Clean Cascade）：不加任何攻击。
    - **评价指标**：
        - 重路由率（Reroute Rate）：被路由到强模型的样本比例（越高说明攻击越有效）。
        - 系统准确率（System Accuracy）：级联最终输出的整体准确率，反映是否保留了回答质量。
- **不确定性度量**：使用了三种不同指标来测试攻击的通用性——答案概率（AP）、逆困惑度（1/PPL）、Jaccard相似度（JS）。

### 4. 资源与算力

- 论文**未明确说明**所使用的GPU型号、数量或具体训练时长。仅提及了一些超参数（如优化器Adam、学习率0.02、训练步数2000/600等）。因此无法给出精确的算力统计。

### 5. 实验数量与充分性

- **实验数量**：较为充分。
    - 两个主要弱模型（Gemma4-E2B、Qwen3-VL-2B）× 三个数据集 × 三个不确定性度量 = 18个主实验组合。
    - 此外还包括：交叉家族级联实验、数据集间迁移实验、预处理防御鲁棒性实验（JPEG压缩、高斯噪声、比特深度降低）、消融实验（边框宽度、温度、目标函数变体）。
    - 消融实验共涉及14个变体设置，并报告了两个延迟率下的准确率。
- **充分性与公平性**：
    - 攻击效果（重路由率）在不同数据集、模型、度量下均一致显著优于清洁级联和两种基线，且接近100%重路由。
    - 基线选择合理：去模糊代表一般图像扰动，提示注入代表文本侧攻击。但未对比更先进的对抗攻击（如FGSM、PGD），作者解释为这是首篇针对级联的攻击，因此只选简单基线。实验设计整体客观，结果清晰地支持论点。

### 6. 主要结论与发现

1. **FDA能有效强制重路由**：在几乎所有设定下，FDA将实际重路由率提升到接近1，即使清洁延迟率很低（如10%），而基线几乎无效。
2. **保留回答质量**：系统准确率在攻击后接近强模型单独运行的准确率，说明触发器的语义保持性好，强模型仍能正确回答。
3. **跨数据集、跨模型迁移性强**：在MMBench上训练的触发器可直接应用于MMMU和ScienceQA，仍实现接近完全重路由；交叉家族级联中，攻击后准确率也接近或超过清洁级联。
4. **抵御常见预处理防御**：JPEG压缩、高斯噪声、比特深度减少均不能完全消除攻击效果。
5. **消融实验验证设计合理**：16像素边框、温度T=3、Border+Temp-KL目标函数组合最佳。

### 7. 优点

1. **问题新颖性**：首次聚焦于级联系统的计算分配攻击，而非答案正确性破坏，开辟了新的安全研究方向。
2. **方法巧妙**：温度平坦化目标函数同时实现度量无关性和语义保持，通用边界触发器设计简单有效且不损害中心内容。
3. **实验全面**：覆盖多种数据集、模型家族、不确定性度量、迁移场景和防御手段，结论可靠。
4. **实用性强**：通用触发器一次训练、处处可用，攻击成本低。

### 8. 不足与局限

1. **基线对比不够强**：仅对比了去模糊和提示注入，未与更先进的对抗攻击（如FGSM、PGD或专为降低置信度的攻击）比较。不过作者承认这是该领域的首篇工作，可视为合理起点。
2. **防御措施有限**：仅测试了三种简单的预处理防御，并未探索更强大的对抗训练或检测方法。实际部署中系统可能采用更复杂的鲁棒机制。
3. **应用场景限制**：仅评估了多选题场景，未扩展到开放生成（如Captioning）或多轮交互。置信度计算方式可能因任务而异。
4. **资源算力未报告**：无法判断该攻击的训练成本，可能对大规模部署造成一定负担。
5. **假设攻击者能访问弱模型logits**：虽使用了开源模型，但实际中服务方可能使用闭源模型，需使用替代模型进行黑盒迁移攻击，论文未充分探讨这一点。
6. **未考虑级联系统动态调整阈值或使用其他路由策略**：若服务方在发现异常后动态调整阈值或引入拒绝机制，攻击效果可能减弱。

（完）
