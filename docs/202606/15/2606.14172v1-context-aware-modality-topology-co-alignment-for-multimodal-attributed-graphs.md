---
title: Context-aware Modality-Topology Co-Alignment for Multimodal Attributed Graphs
title_zh: 上下文感知的模态-拓扑协同对齐用于多模态属性图
authors: "Sirui Zhang, Xu Wang, Zhengyu Wu, Xunkai Li, Hongchao Qin"
date: 2026-06-12
pdf: "https://arxiv.org/pdf/2606.14172v1"
tags: ["query:multi-modal"]
score: 8.0
evidence: 提出模态-拓扑共对齐的多模态图学习方法
tldr: 现有Multimodal Attributed Graphs方法因固定图上下文或统一融合表示，导致任务无关传播和模态信息过度压缩。本文提出CoMAG统一骨干网络，其可靠上下文学习模块基于多模态语义一致性估计边可靠性，并补充语义邻居，通过任务感知门控选择上下文组件；模态保持跳点对齐模块维护模态特定多跳轨迹，跨模态匹配并解耦共享和私有表示。在OpenMAG九个数据集上，CoMAG在图级预测、模态匹配和图条件生成任务中均达到最佳性能，证明任务自适应可靠上下文和模态保持对齐能提升结构预测与跨模态匹配，且保持稀疏边线性复杂度。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.14172v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1824, \"height\": 1015, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14172v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 926, \"height\": 406, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.14172v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1880, \"height\": 495, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.14172v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1889, \"height\": 484, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14172v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 915, \"height\": 515, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14172v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 933, \"height\": 272, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14172v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1866, \"height\": 581, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.14172v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 922, \"height\": 300, \"label\": \"Table\"}]"
motivation: 现有方法依赖固定上下文或统一融合，导致任务无关传播和模态信息丢失，无法同时满足图中心与模态中心任务需求。
method: CoMAG通过可靠上下文学习（基于语义一致性估计边可靠性并补充语义邻居）和模态保持跳点对齐（维护多跳模态轨迹，跨模态匹配并解耦共享私有表示）。
result: 在OpenMAG九个数据集上，CoMAG于图级预测、模态匹配和图表征生成任务中均取得最优结果，超越特征、图、多模态及统一骨干基线。
conclusion: 任务自适应可靠上下文与模态保持对齐有效提升结构预测与跨模态匹配能力，同时保留稀疏边线性复杂度，为多模态属性图提供统一解决方案。
---

## 摘要
多模态属性图通过将图拓扑与文本、图像等异构属性耦合来建模现实世界实体。它们支持需要结构性和类判别性表示的图中心任务，以及需要细粒度跨模态对应的模态中心任务。然而，现有的多模态属性图方法通常依赖固定图上下文或统一融合表示，导致任务无关的传播和过度压缩的融合，阻碍了多样化的任务需求和模态特定证据的保留。为此，我们提出CoMAG，一个统一的多模态属性图主干，学习任务自适应的可靠上下文并在其中进行模态保持的对齐。CoMAG首先通过从多模态语义一致性估计边可靠性、用语义邻居补充原始拓扑、以及通过任务感知门选择上下文组件来执行可靠上下文学习；然后通过保持模态特定的多跳轨迹、跨模态匹配模态-跳令牌、以及解耦共享和私有表示来执行模态保持的跳令牌对齐。因此，CoMAG从一次前向传播中产生图和模态表示，同时保留模态特定的线索。我们进一步分析了稳定传播、缓解过度平滑和控制模态坍塌。在九个OpenMAG数据集上的实验将CoMAG与仅特征、仅图、多模态和统一的多模态属性图基线在图级别预测、模态匹配和图条件生成方面进行比较。结果表明，CoMAG实现了最佳报告性能，展示了任务自适应可靠上下文和模态保持对齐能改进结构预测、跨模态匹配和图条件生成，同时保持稀疏边线性复杂度。

## Abstract
Multimodal Attributed Graphs (MAGs) model real-world entities by coupling graph topology with heterogeneous attributes such as text and images. They support graph-centric tasks requiring structural and class-discriminative representations, and modality-centric tasks requiring fine-grained cross-modal correspondence. However, existing MAG methods often rely on fixed graph contexts or uniformly fused representations, causing task-agnostic propagation and over-compressed fusion that hinder diverse task requirements and modality-specific evidence preservation. To address this, we propose CoMAG, a unified MAG backbone that learns task-adaptive reliable contexts and modality-preserving alignment within them. CoMAG first conducts Reliable Context Learning by estimating edge reliability from multimodal semantic consistency, complementing raw topology with semantic neighbors, and selecting context components through a task-aware gate. It then performs Modality-preserving Hop-token Alignment by maintaining modality-specific multi-hop trajectories, matching modality-hop tokens across modalities, and decoupling shared and private representations. Thus, CoMAG produces graph and modality representations from one forward pass while retaining modality-specific cues. We further analyze stable propagation, over-smoothing mitigation, and modality-collapse control. Experiments on nine OpenMAG datasets compare CoMAG with feature-only, graph-only, multimodal, and unified MAG baselines across graph-level prediction, modality matching, and graph-conditioned generation. Results show that CoMAG achieves the best reported performance, demonstrating that task-adaptive reliable contexts and modality-preserving alignment improve structural prediction, cross-modal matching, and graph-conditioned generation while retaining sparse edge-linear complexity.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **定义**：多模态属性图（MAG）将图拓扑与文本、图像等异构语义属性耦合，建模现实世界实体（如电商产品、社交网络）。
- **现有缺陷**：现有方法通常依赖**固定图上下文**（如原始拓扑或统一学习的拓扑）或**单一融合的模态表示**，导致两重问题：
  1. **任务无关的上下文传播**：不同任务（节点分类、链接预测、跨模态检索）需要不同邻域，而原始图包含噪声边且缺失语义关联，统一传播无法适应。
  2. **过度压缩的跨模态融合**：将多模态强制映射到共同表示空间会抹除模态特有的细节，损害需要细粒度对应关系的模态中心任务（检索、匹配、生成）。
- **本文目标**：设计一个统一MAG主干，既能**学习任务自适应的可靠上下文**，又能在该上下文中**进行模态保持的对齐**，同时服务于图中心任务和模态中心任务。

## 2. 提出的方法论（核心思想、关键技术细节）

### 整体流程
CoMAG包含四个阶段：可靠上下文学习 → 模态特定多跳轨迹传播 → 跳令牌跨模态对齐 → 共享-私有表示解耦。

### 可靠上下文学习
- **边可靠性估计**：利用多模态语义一致性判断边是否可靠。对每条边 (i,j)，计算内部模态一致性和跨模态一致性，通过轻量网络输出可靠性值 R_ij ∈ [0,1]。
- **语义邻居恢复**：基于跨模态语义相似度为每个节点补充高置信度的语义邻居，构建语义补全图 Asem。
- **任务自适应门控**：根据任务嵌入和图级摘要，学习混合权重 π_τ，将可靠拓扑图 Arel、语义补全图 Asem 和自连接 I 融合为最终上下文图 Q_τ。

### 模态特定多跳轨迹传播
- 每个模态在同一上下文图 Q_τ 上独立传播，但使用**任务条件化的传播系数** (γ,α,β) 调节原始信号、同模态上下文、跨模态上下文的权重。
- 更新公式：H^{(m)}_{τ,k+1} = γ_m H^{(m)}_0 + α_m Q_τ H^{(m)}_{τ,k} + β_m Q_τ \bar{H}^{(-m)}_{τ,k}，其中 \bar{H}^{(-m)} 为其他模态的平均隐藏状态。保留所有跳的隐藏状态组成轨迹。

### 跳令牌跨模态对齐
- 将每个（模态，跳）状态视为对齐令牌，添加可学习的模态嵌入和跳位置嵌入。
- 使用距离惩罚的跨模态注意力：B^{m←n}_i = softmax( (Q_i K_i^T)/√d_h - λ_h D_hop )，鼓励相近跳匹配但允许语义强力时的跨跳转移。
- 对齐后的令牌与原始令牌融合。

### 共享-私有表示解耦
- **共享融合**：对每个令牌计算一致性权重，加权融合得到共享表示 s_i，用于图中心任务。
- **私有残差提取**：对每个模态的轨迹池化后减去共享表示的投影，得到私有残差 p^{(m)}_i，保留模态特有证据。
- **输出**：图表示 z^g_i = s_i；模态表示 e^{(m)}_i = Norm(W_s s_i + W_p p^{(m)}_i)。
- **正交正则化**：L_⊥ 惩罚共享与私有表示之间的余弦相似度，防止信息重叠。

### 理论保证
- 定理1：在特定条件下，传播算子收缩，保证收敛到唯一不动点。
- 定理2：残差连接缓解过平滑。
- 命题3：距离惩罚实现有控制的跨跳对齐。
- 命题4：正交解耦限制模态坍塌。

## 3. 实验设计

### 数据集与场景
- 使用 OpenMAG 基准中的 9 个数据集：Movies, Grocery, Toys（电商），DY, KU, Bili_dance（视频推荐），RedditS（社交），Flickr30k（图像网络），SemArt（艺术网络）。均包含文本和视觉两种模态，按 60%/20%/20% 划分训练/验证/测试。

### Benchmark与方法
- **仅特征**：MLP。
- **仅图**：GCN, GAT。
- **多模态图方法**：DMGC, DGF, MMGCN, MGAT, LGMRec。
- **统一骨干**：UniGraph2。
- **提出方法**：CoMAG。

### 任务与指标
- **图级**：节点分类（Accuracy、F1-score），链接预测（MRR、Hits@3），节点聚类（NMI、ARI）。
- **模态级**：模态匹配（AUC、AP），图到文本生成（BLEU-4、CIDEr），图到图像生成（CLIP-S、DINO-S）。

## 4. 资源与算力
- 实验环境：AMD EPYC 7J13 64核CPU，NVIDIA GeForce RTX 4090（24GB显存），CUDA 12.6，Ubuntu 22.04.5 LTS，503GB内存。
- **未说明训练时长**：论文未提及具体训练时间或GPU数量，但可推测使用单卡RTX 4090完成所有实验。

## 5. 实验数量与充分性
- **主要性能对比**：9个数据集 × 2类任务（图级表II、模态级表III），全面覆盖。
- **消融研究**：移除三个关键模块（边可靠性、语义图、私有残差），在Grocery节点分类和Flickr30k图到文本生成上验证。
- **超参数分析**：对λ_⊥和λ_s进行网格搜索（5×5=25组），在Movies和SemArt两个数据集上展示稳定性。
- **鲁棒性分析**：在四种噪声（文本、图像、标签、边）下与6个基线对比，展示CoMAG的鲁棒性。
- **理论复杂度分析**：对比7种方法的训练/推理/内存复杂度。
- **结论**：实验覆盖多任务、多粒度、多噪声场景，设置公平（统一隐藏维度256、dropout 0.3、Adam优化器），消融和鲁棒性分析充分支撑方法有效性。

## 6. 主要结论与发现
- CoMAG在所有9个数据集上，无论是图级任务还是模态级任务，均取得最优或前三的成绩，超越所有基线。
- 边可靠性过滤和语义邻居恢复对图级任务贡献显著；私有残差对模态级任务（尤其是生成）至关重要。
- 任务自适应上下文门控使模型能针对不同任务选择不同上下文，避免“一刀切”。
- 理论分析证实传播稳定、过平滑缓解、模态坍塌可控。
- 复杂度保持稀疏边线性（O(M(|E|+|Q_τ|)d)），适用于大规模图。

## 7. 优点（方法/实验设计的亮点）
- **统一框架**：一个骨干同时输出图表示和模态表示，支持双任务族。
- **模块化设计**：可靠上下文、跳轨迹传播、跳令牌对齐、共享-私有解耦可独立解释和验证。
- **理论深度**：提供收敛性、过平滑缓解、模态坍塌控制的数学证明。
- **实验严谨**：大规模基准（9个数据集）、多种任务、消融、超参数和鲁棒性测试，结果稳定。
- **可重复性**：代码开源（anonymous.4open.science），明确设置细节。

## 8. 不足与局限
- **应用限制**：当前仅验证文本和视觉两个模态，未扩展到音频、视频等多于两种模态的场景。
- **领域覆盖**：现有数据集来自电商、社交、视频推荐等，缺乏科学计算、生物信息等领域的评估。
- **联合训练策略**：论文采用固定权重平衡多个损失，未来可探索自适应损失调度以进一步改善跨任务泛化。
- **算力细节缺失**：未报告训练时间、GPU数量，使得复现和资源需求估算困难。
- **大规模验证不足**：尽管复杂度是线性的，但未在超大规模图（百万节点以上）上验证实际运行效率。
- **消融分析较有限**：仅给出两个数据集的结果，更多的消融组合和跨数据集的全面消融可增强说服力。

（完）
