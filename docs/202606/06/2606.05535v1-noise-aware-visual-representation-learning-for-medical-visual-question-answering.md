---
title: Noise-Aware Visual Representation Learning for Medical Visual Question Answering
title_zh: 面向医学视觉问答的噪声感知视觉表示学习
authors: "I Putu Adi Pratama, Bahadorreza Ofoghi, Atul Sajjanhar, Shang Gao"
date: 2026-06-04
pdf: "https://arxiv.org/pdf/2606.05535v1"
tags: ["query:multi-modal"]
score: 8.0
evidence: 提出噪声感知的多模态医学VQA框架，连接视觉和语言
tldr: 医学视觉问答常忽略图像噪声带来的干扰。本文提出在视觉嵌入映射前加入去噪自编码器，通过重建干净嵌入学习鲁棒表示，再经MLP映射到LLM空间作为视觉前缀，并采用LoRA高效微调。在SLAKE和PathVQA上，该方法显著增强了对噪声的鲁棒性，同时保持干净性能。这为临床决策支持提供了更可靠的视觉问答方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.05535v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1199, \"height\": 837, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.05535v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1244, \"height\": 474, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.05535v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 699, \"height\": 193, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 436, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1089, \"height\": 307, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1096, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1086, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.05535v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1098, \"height\": 311, \"label\": \"Table\"}]"
motivation: 现有Med-VQA方法忽略视觉噪声，导致对图像微小变化敏感，限制临床可靠性。
method: 在视觉嵌入前加入去噪自编码器预训练，重建干净嵌入学习鲁棒表示，经MLP映射为视觉前缀，并采用LoRA高效微调。
result: 在SLAKE和PathVQA上，该方法显著提升噪声鲁棒性，同时保持与现有方法相当的干净性能。
conclusion: 引入去噪自编码器学习鲁棒视觉表示可有效增强Med-VQA对噪声的鲁棒性，推动临床应用。
---

## 摘要
医学视觉问答（Med-VQA）通过使AI模型能够解释医学图像并回答临床相关问题，在临床决策支持方面具有巨大潜力。近期方法通常通过轻量级映射网络将现成的视觉编码器与大语言模型（LLM）连接起来，以降低计算成本。然而，这些方法往往忽略了处理视觉表示中噪声和小幅无关变化的重要性。为应对这些挑战，我们提出了一种噪声感知的Med-VQA框架，该框架在将视觉嵌入映射到LLM输入空间之前，融入了一个去噪自编码器。该去噪自编码器经过预训练，能够从受损输入中重建干净的视觉嵌入，从而鼓励模型学习对噪声不敏感的鲁棒视觉表示。随后，使用多层感知器（MLP）将生成的嵌入投影到语言模型嵌入空间，形成视觉前缀标记，为LLM提供图像信息。为实现无需完全重新训练的高效适配，我们采用低秩适应（LoRA）进行参数高效微调。所提方法在SLAKE和PathVQA基准上进行了评估。实验结果表明，该方法在保持多个评估标准下竞争性干净性能的同时，对噪声输入嵌入具有更强的鲁棒性。这些发现表明，学习更鲁棒的视觉表示可以增强Med-VQA的性能和鲁棒性。

## Abstract
Medical visual question answering (Med-VQA) has strong potential for clinical decision support by enabling AI models to interpret medical images and answer clinically relevant queries. Recent approaches typically connect off-the-shelf vision encoders with large language models (LLMs) through lightweight mapping networks to reduce computational cost. However, these methods often overlook the importance of handling noise and small irrelevant changes in visual representations. To address these challenges, we propose a noise-aware Med-VQA framework that incorporates a denoising autoencoder before visual embeddings are mapped into the input space of an LLM. The denoising autoencoder is pretrained to reconstruct clean visual embeddings from corrupted inputs, encouraging the model to learn robust visual representations that are less sensitive to noise. The resulting embeddings are then projected into the language model embedding space using a multi-layer perceptron (MLP), forming visual prefix tokens that provide image information to the LLM. To enable efficient adaptation without full retraining, we employ parameter-efficient fine-tuning using low-rank adaptation (LoRA). The proposed method is evaluated on the SLAKE and PathVQA benchmarks. Experimental results show improved robustness to noisy input embeddings while maintaining competitive clean performance across multiple evaluation criteria. These findings suggest that learning more robust visual representations can enhance Med-VQA performance and robustness.

---

## 论文详细总结（自动生成）

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：现有的医学视觉问答（Med-VQA）方法通常采用“冻结视觉编码器 + 轻量映射网络 + 大语言模型”的架构，但忽视了视觉表示中存在的噪声和小幅无关变化。这些噪声可能源于医学图像的采集和传输过程，导致投影后的视觉前缀包含干扰信息，影响LLM的生成质量。
- **动机**：提升视觉表示的鲁棒性，使其对噪声不敏感，是增强Med-VQA系统在临床场景中可靠性的关键。然而，在嵌入层面进行显式去噪的探索仍较少。
- **整体含义**：本文提出一种噪声感知框架，在视觉嵌入映射到LLM之前引入去噪自编码器，通过重建任务学习鲁棒表示，从而提高Med-VQA在噪声条件下的性能并保持干净条件下的竞争力。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：两阶段训练范式。第一阶段独立训练去噪自编码器，学习从含噪嵌入重建干净嵌入的鲁棒表示；第二阶段将预训练好的编码器（冻结）集成到Med-VQA流水线中，通过MLP映射为视觉前缀，并用LoRA微调LLM以适配医学领域。
- **关键技术细节**：
  - **视觉编码器**：使用冻结的CLIP ViT-B/32，输出512维全局视觉嵌入。
  - **去噪自编码器**：输入通过添加高斯噪声（σ∈{0.1,0.5,1.0}）得到扰动嵌入，编码器压缩至128维瓶颈，解码器重建回512维。训练使用Smooth L1损失：
    \[
    \mathcal{L}_{\text{DAE}} = \frac{1}{N}\sum_{i=1}^N \begin{cases} \frac{1}{2}(\hat{v}_i - v_i)^2 & |\hat{v}_i - v_i| < 1 \\ |\hat{v}_i - v_i| - \frac{1}{2} & \text{otherwise} \end{cases}
    \]
  - **视觉前缀映射**：预训练后仅保留编码器，将128维潜在表示通过3层MLP（隐藏维度依次为ℓ_x·e/2和ℓ_x·e）映射为8个视觉前缀标记（每个维度1600，对应GPT-2 XL隐藏维度）。
  - **语言模型与微调**：使用冻结的GPT-2 XL作为LLM，输入序列为“question: [问题] context: [视觉前缀] answer: [答案]”。采用LoRA对注意力参数进行参数高效微调，仅更新少量秩分解矩阵。
  - **训练目标**：VQA阶段使用标准的语言建模交叉熵损失 \(\mathcal{L}_{\text{VQA}} = -\sum_{t \in A} \log p_\theta(y_t | y_{<t}, q, I)\)。

## 3. 实验设计
- **数据集**：
  - **SLAKE**：642张放射学图像，14,028个问题，461个唯一答案。包含开放和是非题。
  - **PathVQA**：4,998张病理学图像，32,799个问题，3,182个唯一答案。
- **基准与对比方法**：
  - **Baseline**：直接映射CLIP嵌入到LLM（无中间表示学习）。
  - **Ours w/ AE**：使用标准自编码器（无噪声注入）进行表示学习。
  - **Ours w/ DAE**：本文提出的去噪自编码器方法。
- **评估指标**：BLEU-1、BERTScore、F1分数、准确率。并区分整体准确率及按问题类型（开放/是非）的准确率。
- **鲁棒性评估**：在推理时向CLIP嵌入注入高斯噪声（σ=0.1, 0.5, 1.0），比较各方法的准确率下降情况。同时对比不同阶段噪声水平（σ=0.1,0.5,1.0）对鲁棒性的影响。

## 4. 资源与算力
- 论文未明确说明使用的GPU型号、数量、训练时长等信息。仅提及使用了冻结的CLIP ViT-B/32和GPT-2 XL（XL版本参数量约1.5B），以及LoRA微调。因此无法从原文获取具体算力消耗。

## 5. 实验数量与充分性
- **实验数量**：共报告了：
  - 干净性能对比（表2）：涵盖SLAKE和PathVQA两个数据集，每种方法在Frozen和LoRA两种设置下，4个指标。
  - 不同阶段噪声水平消融（表3、表5）：分别对SLAKE和PathVQA，在LoRA和Frozen下测试σ=0.1,0.5,1.0三种训练噪声下的推理鲁棒性。
  - 鲁棒性对比（表4、表6）：Baseline、AE、DAE在四种推理噪声下的准确率。
  - 定性示例（图2）：展示6组对比。
- **充分性**：实验设计较充分，覆盖了干净和多种噪声强度下的性能，进行了方法消融和训练噪声水平消融。对比方法Baseline和AE的设置与DAE一致（相同映射网络、相同LLM），保证了公平性。但缺少与其他SOTA Med-VQA方法（如MedBLIP、LLaVA-Med等）的直接对比。

## 6. 主要结论与发现
- **鲁棒性显著提升**：在SLAKE上，DAE在LoRA设置下平均噪声准确率（Noisy Avg. Acc.）从Baseline的0.642提升至0.735；在Frozen设置下从0.473提升至0.713。PathVQA上类似提升。
- **中等训练噪声最优**：使用σ=0.5训练DAE取得最佳鲁棒性平衡，而σ=0.1虽在干净条件下更好，但在强噪声下下降更快。
- **干净性能保持或略有改善**：在SLAKE上，DAE在LoRA下达到与AE相同的最高准确率0.756；在PathVQA上，DAE的干净准确率略低于AE（0.592 vs 0.602），但鲁棒性大幅领先。
- **去噪表示优于直接投影和标准自编码器**：在所有噪声水平下，DAE均优于Baseline和AE，尤其在强噪声（σ=1.0）下优势更明显。

## 7. 优点
- **方法创新**：首次在Med-VQA中系统性地将嵌入级去噪自编码器引入视觉表示学习，并提出两阶段训练范式（先学习鲁棒表示，再适配下游任务），设计清晰。
- **鲁棒性评估系统化**：在推理时向嵌入注入不同强度的高斯噪声，提供了一种可控且可复现的鲁棒性评估协议，有助于公平比较。
- **参数高效**：使用LoRA微调LLM，避免全参数微调，适合医学领域小标注数据场景。
- **消融实验完整**：分别研究训练噪声水平和推理噪声水平的影响，验证了去噪策略的有效性。

## 8. 不足与局限
- **噪声类型单一**：仅使用高斯噪声模拟嵌入扰动，未考虑实际医学图像中的结构化噪声（如泊松噪声、运动伪影、设备噪声等），可能低估了真实场景的复杂性。
- **视觉映射简单**：使用3层MLP作为映射网络，未探索更复杂的对齐机制（如Q-Former、交叉注意力），虽保持轻量但可能限制表示潜力。
- **模型规模有限**：仅测试了GPT-2 XL（约1.5B）和CLIP ViT-B/32，未在更大LLM（如LLaMA）或更强的视觉编码器（如BiomedCLIP）上验证泛化性。
- **缺乏与其他SOTA方法的直接比较**：仅与自身Baseline和AE对比，未与LLaVA-Med、MedBLIP等近期强基线在同一设置下比较，外部有效性稍弱。
- **计算资源未报告**：缺乏关于GPU型号、训练时长等算力信息，影响可复现性评估。

（完）
