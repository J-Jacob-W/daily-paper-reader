---
title: "Motion Reinforces Appearance: RGB-Skeleton Gated Residual Fusion for Micro-Gesture Online Recognition"
title_zh: 运动增强外观：基于RGB-骨架门控残差融合的微手势在线识别
authors: "Jialin Liu, Xinwen He, Pengyu Liu, Jiale Shi, Huaijuan Zang, Yanbin Hao"
date: 2026-06-10
pdf: "https://arxiv.org/pdf/2606.11645v1"
tags: ["query:multi-modal"]
score: 8.0
evidence: RGB-骨架门控残差融合用于多模态微手势识别
tldr: 微手势在线识别任务中，仅依赖单模态难以捕捉完整线索。本文提出DyFADet+双流RGB-骨架框架，通过门控残差融合模块将骨架运动自适应注入RGB表示，而非简单拼接。在SMG数据集上F1达40.88，排名第二。该方法有效结合了外观与运动信息，提升了微手势识别的准确性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.11645v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1216, \"height\": 657, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.11645v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1257, \"height\": 680, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.11645v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1271, \"height\": 418, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.11645v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 745, \"height\": 323, \"label\": \"Table\"}]"
motivation: 单模态线索不完整，需融合RGB与骨架以捕捉细微运动。
method: 双流RGB-骨架框架，门控残差融合自适应注入骨架运动。
result: SMG数据集F1=40.88，排名第二。
conclusion: 门控残差融合有效增强微手势识别性能。
---

## 摘要
微手势分析通过细微的身体动作推断自发情绪，日益受到关注。微手势在线识别（MGR）旨在从无修剪视频中定位并分类每个手势实例，是第四届EI-MiGA-IJCAI挑战赛的核心任务。与典型的时序动作检测相比，MGR强调动作的定位与分类，要求模型输出每个微手势的开始时间、结束时间和类别。此外，由于微手势高度自发，仅依赖单一模态难以捕捉完整准确的多模态线索。本文提出DyFADet+，将DyFADet扩展为双流RGB-骨架框架。在我们的模型中，两种模态均被投影到共享的多尺度时序嵌入中，并通过门控残差模块进行融合，该模块自适应地将骨架运动注入RGB表示中，而非简单拼接。最后，这些融合特征由动态TAD头解码，用于在线分类和边界回归。在SMG数据集上，我们的方法取得了40.88的F1分数，在微手势在线识别赛道中排名第二。

## Abstract
Micro-gesture analysis attracts increasing attention for inferring spontaneous emotion from subtle body movements. Micro-gesture online recognition, which localizes and classifies each gesture instance in untrimmed videos, is a core task in the 4th EI-MiGA-IJCAI Challenge. Compared with typical temporal action detection, MGR emphasizes the localization and classification of actions, requiring the model to output the start time, end time, and category of each micro-gesture. Moreover, since micro-gestures are highly spontaneous, relying solely on a single modality makes it difficult to capture the complete and accurate multi-modal cues. In this work, we propose DyFADet+, which extends DyFADet into a dual-stream RGB-skeleton framework. In our model, both modalities are projected into shared multi-scale temporal embeddings and fused through a gated residual module, which adaptively injects skeleton motion into the RGB representation rather than using naive concatenation. Finally, these fused features are decoded by a Dynamic TAD head for online classification and boundary regression. On the SMG dataset, our method achieves an F1 score of 40.88, ranking 2nd in the Micro-gesture Online Recognition track.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：微手势在线识别（Micro-Gesture Online Recognition, MGR）要求从无修剪视频中同时定位并分类每个手势实例（预测开始时间、结束时间和类别）。微手势具有动作幅度小、边界模糊、易被无关动作遮挡的特点，单模态（如仅RGB或仅骨架）难以提供完整的线索。
- **动机**：RGB包含丰富外观细节但缺乏可靠运动线索；骨架提供精确的关节运动信息但容易因微小位移而噪声大。现有融合方法多采用简单拼接或早期融合，易放大姿态估计误差，破坏跨模态互补性。
- **整体含义**：本文旨在通过一种自适应、可学习的融合方式，选择性利用骨架运动增强RGB特征，从而提升微手势在线识别的准确性和鲁棒性。

## 2. 方法论
- **核心思想**：将单模态时序检测器DyFADet扩展为双流RGB-骨架框架，通过门控残差融合模块取代简单拼接，实现模态间的选择性信息注入。
- **关键技术细节**：
  - **双流特征提取**：
    - RGB分支：使用VideoMAEv2-g提取特征，经浅层时序卷积和Dynamic Encoder块（DynE）多尺度下采样，得到金字塔 `{F_r^(ℓ)}`。
    - 骨架分支：使用预计算的运动表示，经1D卷积嵌入 + 卷积块（Conv blocks）逐步下采样，生成对齐的多尺度金字塔 `{F_s^(ℓ)}`。当骨架时间长度较短时，用最近邻插值上采样对齐。
  - **门控残差融合（每层级ℓ）**：
    - 门控：`G^(ℓ) = σ( φ( [F_r^(ℓ), F_s^(ℓ)] ) )`，其中`[·,·]`为通道拼接，`φ`为1×1时序卷积，`σ`为sigmoid。
    - 融合特征：`F_f^(ℓ) = F_r^(ℓ) + α · G^(ℓ) ⊙ ψ(F_s^(ℓ))`，其中`ψ`为两层GELU卷积适配器，`α=0.5`缩放残差。
    - 门控卷积的偏置初始化为-2，使门控初始输出接近零，优先依赖RGB通道稳定训练。
  - **时序检测头**：融合后的多尺度金字塔输入动态TAD头（DyHead），它动态调整参数并融合跨层级特征。输出分别经过分类分支（1D卷积+sigmoid预测17类概率）和回归分支（1D卷积+ReLU预测到边界的距离），使用预定义步长{1,2,4,8,16,32}和对应回归范围。
- **损失函数**：分类用focal loss，边界回归用DIoU loss，端到端优化。

## 3. 实验设计
- **数据集**：SMG数据集，包含3692个微手势实例，17个类别，来自40名受试者。分为训练30人、验证5人、测试5人。提供RGB视频和骨架序列。
- **基准与对比方法**：
  - 对比了MiGA挑战赛2023–2026历届最佳方案，包括单模态（RGB或骨架）和RGB+骨架融合方法，如NPU-Stanford（骨架+多尺度Transformer）、HFUT-VUT（RGB+I3D+Mamba）、XInsight Lab（RGB+AdaTAD）等。
  - 消融实验：RGB only、Skeleton only、RGB+Skeleton简单拼接、本文方法。
- **评价指标**：F1分数（综合考虑精确率和召回率）。

## 4. 资源与算力
- **文中未明确说明使用的GPU型号、数量及训练时长**。仅提到训练配置：batch size=2（验证/测试=1），200个epoch，学习率1e-4，余弦退火，20轮线性预热，AdamW优化器。未提供计算资源细节，无法判断算力需求。

## 5. 实验数量与充分性
- **实验数量**：主要包含一组消融实验（表2，共4个变体）和排行榜对比（表1，7种方法）。额外的超参数或模态分析未展示。
- **充分性与客观性**：消融设计合理，直接验证了门控残差融合的优势。对比结果来自官方排行榜，公平客观。但仅在一个数据集（SMG）上验证，未在iMiGUE、MA-52等其它微手势数据集上进行跨数据集测试，泛化性有待进一步验证。

## 6. 主要结论与发现
- 门控残差融合（F1=40.88）明显优于RGB only（38.76）和简单拼接（38.96），证明自适应选择可靠骨架运动信息能有效提升检测性能。
- 方法在MiGA 2026挑战赛排名第二，且超越2025年冠军（38.03），说明骨架运动增强在多模态微手势识别中具有竞争力。
- 单独使用骨架效果较差（22.31），表明RGB仍是主要信息源，骨架仅起辅助作用。

## 7. 优点
- **融合创新**：门控残差融合通过可学习的门控机制自适应选择可靠的骨架运动，避免噪声注入，比简单拼接更稳健。
- **稳定训练**：将门控偏置初始化为-2，使网络初期主要依赖RGB，防止骨架噪声破坏训练，对低质量姿态估计具有抵抗力。
- **双流架构轻量高效**：骨架分支使用轻量卷积块，与RGB分支对齐并融合，计算开销可控。
- **基于强基线的扩展**：采用成熟的DyFADet作为基础检测器，继承其动态特征聚合优势，降低了工程复现难度。

## 8. 不足与局限
- **实验验证有限**：仅在SMG一个数据集上进行，未在其他微手势数据集（如iMiGUE、MMA-52）或宏动作数据集上验证泛化性。
- **未分析骨架噪声的影响**：未定量分析姿态估计误差如何影响融合效果，以及门控机制对不同噪声水平的稳定性。
- **未与更多TAD方法对比**：消融仅对比了RGB、骨架和简单拼接，未与基于Transformer的其他融合方案（如多头注意力融合）对比。
- **长尾类别性能缺失**：论文未报告各类别（特别是稀有手势）的独立F1，可能掩盖类别不平衡问题。
- **计算资源未报告**：对可重复性造成一定障碍，且无法比较与其他方法的效率。

（完）
