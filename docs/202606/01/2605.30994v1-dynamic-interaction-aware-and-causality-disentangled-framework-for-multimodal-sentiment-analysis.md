---
title: Dynamic Interaction-Aware and Causality-Disentangled Framework for Multimodal Sentiment Analysis
title_zh: 动态交互感知与因果解耦的多模态情感分析框架
authors: "Guangyuan Dong, Ziwei Hong, Shenghao Liu, Chenyu Wu, Yuanyuan Fang, Zihao Li, Xudong Zhang, Bingchen Liu, Yuchen Zhang, Haitao Ding, Zhenzhou Zhou, Ziyu Song"
date: 2026-05-29
pdf: "https://arxiv.org/pdf/2605.30994v1"
tags: ["query:multi-modal"]
score: 8.0
evidence: 多模态情感分析融合框架
tldr: "多模态情感分析面临静态冲突抑制无法适应动态变化、语言模态固有情感偏见纠缠其他模态的问题。本文提出MCAF框架，通过信息瓶颈原理构建结构因果模型解缠语言偏见，得到纯引导信号；设计动态多模态路由器实时评估交互状态并自适应分配权重；利用条件扩散去噪模块迭代过滤残差无关信息。在CMU-MOSI和CMU-MOSEI上取得新SOTA，Acc-2/F1分别达86.52%/86.51%和86.72%/86.65%，验证了动态感知交互、解缠偏见和可解释性的有效性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.30994v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1241, \"height\": 1291, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30994v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1576, \"height\": 612, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30994v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 727, \"height\": 1062, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30994v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 866, \"height\": 402, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.30994v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 865, \"height\": 373, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.30994v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 772, \"height\": 155, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30994v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1838, \"height\": 595, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30994v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 744, \"height\": 423, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.30994v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1979, \"height\": 293, \"label\": \"Table\"}]"
motivation: 现有方法无法动态适应样本间变化，且语言模态的固有情感偏误会误导其他模态学习。
method: 提出MCAF，包括因果干预模块基于信息瓶颈解缠语言偏见，动态多模态路由器调节交互，条件扩散去噪模块生成鲁棒超模态表示。
result: "在CMU-MOSI和CMU-MOSEI上分类指标Acc-2/F1分别达86.52%/86.51%和86.72%/86.65%，超越现有方法。"
conclusion: MCAF通过动态感知交互和解缠偏见，增强了多模态情感分析的可解释性和鲁棒性。
---

## 摘要
尽管多模态情感分析（MSA）有效利用了语言、视觉和声学模态的丰富信息，现有方法仍面临两个核心挑战：1）静态冲突抑制机制无法适应样本间的动态变化；2）语言模态固有的情感偏差会误导其他模态的学习，且该偏差尚未解耦。为此，我们提出动态多模态因果解耦与自适应融合框架（MCAF）。其基石是多粒度因果动态路由器和条件扩散去噪模块。首先，我们引入基于信息瓶颈原理的因果干预模块，构建结构因果模型将情感偏差从语言特征中解耦，得到“去偏”语言表征作为纯引导信号。其次，我们设计动态多模态路由器，实时评估视觉、声学与去偏语言信号在特征、时间步和模态三个层面的交互状态（互补、冲突或冗余），然后自适应分配权重并路由信息流以实现细粒度调控。最后，轻量级条件扩散去噪模块对融合后的联合表征进行迭代去噪，显式滤除残留无关信息，生成鲁棒的超级模态表征。在CMU-MOSI和CMU-MOSEI基准上的大量实验表明，MCAF在关键分类指标上创下新纪录，MOSI上达到Acc-2/F1为86.52%/86.51%，MOSEI上为86.72%/86.65%，同时在其他指标上保持高度竞争力。全面的分析与可视化进一步验证了其在动态感知交互、解耦偏差和增强可解释性方面的有效性。

## Abstract
Although Multimodal Sentiment Analysis (MSA) effectively leverages rich information from language, visual, and acoustic modalities, existing methods still face two core challenges: 1) static conflict suppression mechanisms fail to adapt to dynamic variations across samples, and 2) the inherent sentimental bias within the language modality, which can misguide learning from other modalities, remains entangled. To this end, we propose a Dynamic Multimodal Causal Disentanglement and Adaptive Fusion Framework (MCAF). Its cornerstone is the Multi-Granularity Causal Dynamic Router and a Conditional Diffusion Denoising Module. First, we introduce a causal intervention module based on the information bottleneck principle, which builds a Structural Causal Model to disentangle sentimental bias from language features, yielding a "de-confounded" language representation as a pure guiding signal. Second, we devise a Dynamic Multimodal Router that evaluates the interaction states (complementary, conflicting, or redundant) among visual, acoustic, and de-confounded language signals in real-time across three levels: feature, temporal, and modality, then adaptively allocates weights and routes information flow for fine-grained regulation. Finally, a lightweight Conditional Diffusion Denoising Module performs iterative denoising on the fused joint representation to explicitly filter out residual irrelevant information, generating a robust hyper-modality representation. Extensive experiments on the CMU-MOSI and CMU-MOSEI benchmarks show that MCAF sets new state-of-the-art on key classification metrics, achieving an Acc-2/F1 of 86.52%/86.51% on MOSI and 86.72%/86.65% on MOSEI, while remaining highly competitive on others. Comprehensive analyses and visualizations further validate its efficacy in dynamically perceiving interactions, disentangling bias, and enhancing interpretability.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：多模态情感分析（MSA）通过融合语言、视觉、声学模态信息提升情感识别性能，但现有方法存在两大瓶颈：
  - **静态冲突抑制**：现有方法依赖全局训练的静态权重，无法适应不同样本甚至同一样本不同时刻的交互状态（互补、冲突、冗余）的动态变化。例如讽刺表达中正面语调与负面文本的冲突恰恰是关键线索，不应被简单抑制。
  - **语言模态固有偏见**：语言特征常作为引导信号，但其本身携带强烈的先验情感倾向（如文化、说话者习惯），导致模型不恰当地压制视觉/声学模态中表面矛盾但实际有价值的细微情感线索（如微表情、语调变化），尤其在讽刺、反语等场景中问题突出。

- **核心思想**：将MSA建模为**动态交互感知与因果解耦**过程，提出MCAF框架，旨在解决静态抑制和语言偏见问题，实现更鲁棒、可解释的情感分析。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 整体架构（MCAF）
由四个顺序阶段组成：初始单模态特征处理 → 因果引导模态解耦（CGMD） → 动态多模态交互路由器（DMIR） → 条件扩散去噪模块（CDDM） → 分类/回归输出。

### 关键技术细节

#### a. 因果引导模态解耦（CGMD）
- **目的**：将语言特征中的语义内容与情感偏见分离，得到“纯净”引导信号。
- **方法**：
  - 双分支编码器：语义编码器 \( E_s \) 提取语义因子 \( Z_s \)，偏见编码器 \( E_b \) 提取偏见因子 \( Z_b \)。
  - 基于结构因果模型（SCM）：\( U_l = G(Z_s, Z_b) + \epsilon \)。
  - 干预（do- calculus）：批内打乱偏见因子 \( Z'_b = \text{Shuffle}(Z_b) \)，模拟反事实场景。
  - 解码器 \( D \) 重建原始及干预后的语言表示。
- **优化约束**（三个损失）：
  - 重建保真度损失 \( L_{\text{recon}} \)：最小化重建误差。
  - 因子独立性损失 \( L_{\text{ind}} \)：通过HSIC（希尔伯特-施密特独立准则）强制 \( Z_s \) 与 \( Z_b \) 统计独立。
  - 跨模态对齐损失 \( L_{\text{align}} \)：对比学习最大化语义因子与视觉/声学表示的互信息。
- **推理时**：仅保留语义编码器 \( E_s \)，输出净化表征 \( L_{\text{pure}} \)。

#### b. 动态多模态交互路由器（DMIR）
- **目的**：实时评估三模态交互状态，自适应路由信息流。
- **三个粒度评估**：
  - **特征级**：交叉注意力分布 \( \alpha_{lv}, \alpha_{la}, \alpha_{va} \)。
  - **时间步级**：双向GRU捕获时序动态，计算余弦相似度矩阵 \( C_{lv}, C_{la}, C_{va} \)。
  - **模态级**：全局统计量（均值、标准差、熵、稀疏度） \( s_m \)。
- **路由生成**：
  - 通过MLP融合上述特征得到 \( F_{\text{int}} \)。
  - 门控机制（sigmoid+s温控退火）生成稀疏路由矩阵 \( R \in \mathbb{R}^{3\times 3} \)。
  - 稀疏线性组合生成调整后的模态表示 \( e_L, e_V, e_A \)。
- **输出**：拼接三个调整后的表示，经全局平均池化得到紧凑融合向量 \( f_{\text{global}} \)，再通过线性层输出分类和回归结果。

#### c. 条件扩散去噪模块（CDDM）
- **目的**：对融合后的联合表示进行迭代去噪，滤除残留无关信息。
- **方法**：轻量级条件扩散模型，以 \( f_{\text{global}} \) 为条件进行前向加噪和反向去噪，生成鲁棒的“超级模态”表示（论文中提及但未详细展开算法细节，主要强调其存在性）。

### 公式/算法流程（文字说明）
1. 输入三种模态原始特征 \( U_l, U_v, U_a \)。
2. 通过预训练BERT和Transformer提取 \( x_l, x_v, x_a \)，投影至统一维度 \( d=128 \)。
3. CGMD模块：双分支编码 → 干预 → 重建 + 独立性与对齐损失 → 输出 \( L_{\text{pure}} \)。
4. DMIR模块：计算三类评估 → MLP融合 → 生成稀疏路由矩阵 \( R \) → 加权组合得到 \( e_L, e_V, e_A \) → 拼接 + 池化 → \( f_{\text{global}} \)。
5. CDDM模块：基于 \( f_{\text{global}} \) 进行扩散去噪 → 最终表示 → 分类器输出。
6. 训练目标：CGMD损失 \( L_{\text{CGMD}} \)（含三个子损失）+ 情感分类/回归损失。

## 3. 实验设计

### 数据集
- **CMU-MOSI**：2198个视频片段（训练1283/验证229/测试686），情感强度范围[-3, +3]。
- **CMU-MOSEI**：22852个视频片段（训练16322/验证1871/测试4659），更大更复杂。

### 评估指标
- Acc-2（二分类准确率）、F1（二分类）、Acc-7（7分类准确率）、MAE（平均绝对误差）、Corr（皮尔逊相关系数）。

### 对比方法
- 早期融合：EF-LSTM
- 晚期融合：LF-DNN
- 张量融合：TFN、LMF
- 跨模态注意力：MULT
- 表示学习：MISA、Self-MM、TETFN
- 元学习：AMML
- 信息压缩：MCEN

### 实现细节
- 文本：BERT-base（768维）；音频：Librosa（20维）；视频：OpenFace（5维）。
- 所有特征对齐至T=50个时间步。
- 隐藏维度d=128；优化器AdamW；MOSI：batch=64，epoch=30，lr=1e-4；MOSEI：batch=64，epoch=15，lr=5e-5。

## 4. 资源与算力
- **文中未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。仅提及优化器为AdamW，未报告训练时间或硬件配置。

## 5. 实验数量与充分性

### 实验组数
- **主对比实验**：在MOSI和MOSEI两个数据集上与10种方法比较（表II）。
- **消融实验**：在MOSI和MOSEI上进行三个变体（w/o CGMD, w/o DMIR, w/o CGMD&DMIR）与完整模型的对比（表IV）。
- **模态组合实验**：在MOSI上测试单模态（T/A/V）、双模态（A+V, T+A, T+V）、三模态（T+A+V）的性能（表III）。
- **可视化分析**：收敛曲线（图4）、误差分布对比（图5）。

### 充分性与公平性
- **充分**：覆盖了基础方法、先进方法，以及核心模块的消融。模态组合实验验证了各模态贡献。
- **客观公平**：采用统一特征提取和预处理流程（与[23]一致），对比方法使用官方或公认实现结果。但未披露对比方法的复现细节，可能存在少许偏差。

## 6. 论文的主要结论与发现

- **性能突破**：MCAF在MOSI上Acc-2/F1达86.52%/86.51%，在MOSEI上达86.72%/86.65%，均超越此前最优MCEN。
- **动态路由有效性**：DMIR能根据实时交互状态（互补/冲突/冗余）自适应调节信息流，优于静态抑制机制。
- **因果解耦有效性**：CGMD有效去除语言偏见，提升跨模态融合纯度。
- **消融验证**：移除任意核心模块均导致性能显著下降，同时移除时退化最严重，证明两者互补。
- **可视化证据**：误差分布更集中（标准差降低约33%），收敛更快更稳，在极端情感区域优势明显。

## 7. 优点

- **问题洞察深刻**：首次明确指出静态抑制和语言偏见两大挑战，并给出因果动态视角的解决方案。
- **方法创新性**：将因果干预（do-calculus）、信息瓶颈、动态路由、扩散去噪有机结合，形成完整框架。
- **模块设计精细**：CGMD的HSIC独立性约束+跨模态对比对齐，DMIR的三级交互评估+稀疏门控路由，均具备理论支撑。
- **实验充分**：涵盖两个主流数据集、多个指标、详尽的消融和模态分析，可视化验证增强可信度。
- **可解释性强**：动态路由矩阵可直观展示交互模式，因果解耦可分离偏见，提升了模型透明度。

## 8. 不足与局限

- **回归指标并非最优**：在MOSI和MOSEI上MAE和Corr略逊于MCEN（但差距很小），表明模型在连续情感强度估计上仍有提升空间。
- **扩散去噪模块细节缺失**：论文对CDDM仅简要提及，未给出具体的扩散步数、噪声调度、条件融入方式等，复现难度大。
- **MOSEI上ACC-7不敌MCEN**：在更细粒度分类（7类）上能力较弱，可能说明动态路由在复杂多样样本上不够稳定。
- **算力资源未报告**：缺少训练时间、GPU型号等，不利于评估方法部署成本。
- **应用范围有限**：仅在英文电影评论数据集上验证，未测试跨语言、跨领域（如对话、直播）场景的泛化性。
- **偏见分析依赖实验数据集**：CMU-MOSI/MOSEI中的偏见模式可能与真实场景有差异，因果解耦效果需进一步在人工设计偏见数据上验证。

（完）
