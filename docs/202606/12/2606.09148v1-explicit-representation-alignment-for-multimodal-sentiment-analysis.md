---
title: Explicit Representation Alignment for Multimodal Sentiment Analysis
title_zh: 多模态情感分析中的显式表示对齐
authors: "Baode Wang, Ziming Wang, Huacan Wang, Ronghao Chen, Biao Wu"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.09148v1"
tags: ["query:multi-modal"]
score: 9.0
evidence: 提出一种多模态融合框架，在融合前对齐表示用于情感分析，直接涉及多模态融合技术
tldr: 多模态情感分析中，预训练编码器间的表示不对齐是模型难以超越文本基线的关键瓶颈。本文提出利用视觉语言模型将图像转为结构化文本，对齐到统一语言空间，并设计语义token选择与批次级均匀性正则化的混合学习策略，抑制噪声。在多个基准上，该方法显著优于现有单模态与多模态方法，表明对齐比复杂融合更重要。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.09148v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 776, \"height\": 397}, {\"url\": \"assets/figures/arxiv/2606.09148v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1565, \"height\": 646}, {\"url\": \"assets/figures/arxiv/2606.09148v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1613, \"height\": 344}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.09148v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 791, \"height\": 573}, {\"url\": \"assets/tables/arxiv/2606.09148v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 794, \"height\": 503}, {\"url\": \"assets/tables/arxiv/2606.09148v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 803, \"height\": 332}, {\"url\": \"assets/tables/arxiv/2606.09148v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 753, \"height\": 296}]"
motivation: 表示不对齐导致多模态模型性能不稳定，对齐融合前比融合复杂度更关键。
method: 利用VLM将视觉内容转为文本描述，结合语义token选择和批次级均匀性正则化训练。
result: 在多个情感和情绪基准上取得SOTA，优于单模态和强多模态基线。
conclusion: 表示对齐是多模态情感学习的关键，所提框架有效提升鲁棒性。
---

## 摘要
多模态情感分析旨在通过联合建模文本和图像等异构模态来理解人类情感和情绪。然而，多模态模型往往无法持续优于强大的纯文本基线，其性能在不同融合策略下差异显著。在本文中，我们识别出独立预训练的模态编码器之间的表示未对齐是阻碍有效多模态学习的关键瓶颈，并通过控制实验表明，融合前的对齐通常比融合复杂性更为重要。为解决此问题，我们提出一个统一的多模态情感分析框架，利用视觉-语言模型（VLM）将视觉内容转换为结构化的文本描述，将异构模态投影到共享的语言空间，实现可解释的以文本为中心的推理。为进一步提升鲁棒性，我们引入一种混合学习策略，结合语义标记选择与批次级均匀性正则化目标，鼓励更分散和稳定的全局特征空间，同时减轻VLM生成描述引入的噪声。在多个多模态情感和情绪基准上的实验表明，我们的方法持续优于强大的单模态和多模态基线，达到最先进性能。我们的分析进一步强调了表示对齐在多模态情感学习中的关键作用。

## Abstract
Multimodal affective analysis aims to understand human sentiment and emotion by jointly modeling heterogeneous modalities such as text and images. However, multimodal models often fail to consistently outperform strong text-only baselines, with performance varying significantly across fusion strategies. In this work, we identify representation misalignment between independently pretrained modality encoders as a key bottleneck for effective multimodal learning, and show through controlled experiments that alignment prior to fusion is often more important than fusion complexity. To address this issue, we propose a unified multimodal affective analysis framework that leverages vision-language models (VLMs) to convert visual content into structured textual descriptions, projecting heterogeneous modalities into a shared linguistic space and enabling interpretable text-centric reasoning. To further improve robustness, we introduce a hybrid learning strategy that combines semantic token selection with a batch-level uniformity regularization objective, encouraging a more dispersed and stable global feature space while mitigating noise introduced by VLM-generated descriptions. Experiments on multiple multimodal sentiment and emotion benchmarks show that our method consistently outperforms strong unimodal and multimodal baselines, achieving state-of-the-art performance. Our analysis further highlights the critical role of representation alignment in multimodal affective learning.

---

## 论文详细总结（自动生成）

# 中文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **核心问题**：多模态情感分析中，虽然理论上有望比纯文本模型捕捉更丰富的情感线索，但实际中多模态模型却常常无法稳定地超越强大的文本基线，甚至性能差异很大。作者通过分析发现，**独立预训练的模态编码器（如BERT、ResNet）之间的表示未对齐是根本瓶颈**，即使采用复杂融合架构也难以弥补这种语义鸿沟。
- **背景意义**：多模态情感分析在人机交互、对话系统、用户建模等领域至关重要。当前主流方法依赖独立编码器融合，但忽视了特征空间的不一致性（如尺度、分布、几何结构差异），导致模型偏向于依赖某一模态（通常是文本），限制了跨模态互补效果。

## 2. 方法论

### 核心思想
提出**统一多模态情感分析框架（UMAF）**，在融合前通过视觉-语言模型（VLM）将视觉内容显式转换为结构化文本，使异构模态对齐到共享的语言空间，实现“以文本为中心”的可解释推理，并设计噪声鲁棒性增强策略。

### 关键技术细节

1. **多模态表示提取**：设计结构化推理提示（prompt），包含OCR理解、视觉场景分析、跨模态整合、高层推理四个步骤，利用VLM（如LLaVA等）将图像转化为结构化文本描述（`Tvlm`）。
2. **跨模态融合**：
   - 将原始文本 `T` 与VLM生成的描述 `Tvlm` 拼接，送入RoBERTa编码得到 `Hbase`。
   - 在RoBERTa之上添加两个轻量Transformer层进行显式交互，输出融合表示 `Z`。
3. **Top-K语义标记选择**：使用可学习的打分网络计算每个token的重要性，选取得分最高的K个token进行平均池化，得到判别性表示 `htop`，用于分类损失 `Lce`。
4. **批次级均匀性正则化**：引入 `Luni` 损失，鼓励同一批次内不同样本的全局表示在特征空间更分散，防止特征坍缩，提高对VLM噪声的鲁棒性。
5. **总损失**：`Ltotal = Lce + λLuni`，其中λ平衡分类监督与正则化。

## 3. 实验设计

### 数据集
- **MSED**：9,190个图像-文本对，标注六类欲望、三类情感极性、六类情绪。70%/10%/20%划分。
- **MVSA-Single**：4,869个图像-文本对，用于情感分析（单标注）。

### 对比方法
- 在MSED上对比了CoMN、FENet、CM-BERT、MAG、MVAN、ITIN、DMLANet、MDPF-CLIP等。
- 在MVSA上对比了CoMN、FENet、CM-BERT、MAG、MVAN、ITIN、DMLANet、MDPF-CLIP等。
- 消融实验还对比了不同编码器组合（BERT+ResNet、RoBERTa+ViT、CLIP文本/多模态等）及噪声鲁棒模块、不同K值。

## 4. 资源与算力

论文**未明确说明**使用的GPU型号、数量、训练时长等具体算力信息。仅在局限性中提到“VLM生成描述会引入额外计算成本”，但未量化。

## 5. 实验数量与充分性

- **主实验**：两个数据集（MSED和MVSA-Single）共2组主要结果表格。
- **消融实验**：
  - 不同编码器组合（表3）验证模态对齐重要性。
  - 噪声鲁棒特征模块消融（表4），包含移除NRF、不同K值（5/10/20）对比。
- **进一步分析**：注意力分布可视化（图3），直观展示对齐前后模态注意力模式变化。
- **充分性评价**：实验设计较为完整，覆盖了方法核心组件（VLM对齐、Top-K选择、均匀性正则化），并进行了多维度消融。但缺少在更多样化场景（如视频、语音）上的验证，且对比的基线均为公开方法，公平性较好。

## 6. 主要结论与发现

1. **表示对齐比融合复杂性更重要**：融合前将视觉转换为文本可显著提升性能，且对齐后的模型注意力分布更均衡，避免了单模态偏置。
2. **UMAF框架持续超越强基线**：在MSED上，情感分析F1达89.46%，情绪识别F1达87.09%；在MVSA-Single上准确率82.3%，F1 81.8%，均优于所有对比方法。
3. **噪声鲁棒特征有效**：移除噪声鲁棒模块或使用过大K值均导致性能下降，验证了Top-K选择与均匀性正则化的作用。
4. **CLIP的多模态对齐能力优于独立编码器组合**：CLIP文本+图像搭配效果远超BERT+ResNet或RoBERTa+ViT，进一步证明预训练跨模态对齐的价值。

## 7. 优点

- **创新性**：明确指出表示不对齐是核心瓶颈，并提出将视觉显式文本化的对齐策略，避免复杂融合结构。
- **可解释性**：通过结构化prompt和注意力可视化，中间推理过程可追踪，提升了模型透明度。
- **鲁棒性**：Top-K选择和批次级均匀性正则化简单有效，缓解了VLM生成噪声问题。
- **实验充分**：在两个基准上均达到SOTA，并做了多角度消融和可视化分析，结论可信。

## 8. 不足与局限

- **模态覆盖不全**：仅聚焦文本-图像，未考虑语音、视频等真实多模态场景。
- **依赖VLM质量**：VLM生成描述的噪声或遗漏可能影响下游性能，存在累积误差风险。
- **额外计算开销**：VLM推理和两阶段训练增加了计算成本，但论文未量化。
- **泛化性待验证**：仅在社交平台数据集上验证，现实多样性（如不同领域、文化背景）场景下的表现未知。
- **未开源**：未提及代码或模型公开计划，不利于复现和后续研究。

（完）
