---
title: "Pareto LoRA: Mitigating Modality Imbalance in Unified Multimodal Models via Pareto-Optimal Gradient Integration"
title_zh: Pareto LoRA：通过帕累托最优梯度集成缓解统一多模态模型中的模态不平衡
authors: "Xiwen Wei, Mark Nutter, Madhusudhanan Srinivasan, Radu Marculescu"
date: 2026-06-15
pdf: "https://arxiv.org/pdf/2606.17296v1"
tags: ["query:multi-modal"]
score: 9.0
evidence: 多模态融合技术解决模态不平衡
tldr: "统一多模态模型（UMMs）在指令微调中面临模态不平衡问题，语言梯度主导导致图像生成质量严重下降，尤其在LoRA参数高效微调中。本文系统分析了LoRA微调下的梯度差异，发现视觉梯度在某些层比语言梯度小数个数量级。基于此，提出Pareto LoRA，将多模态指令微调视为双目标优化，通过帕累托最优梯度方向与强度调制来平衡文本和图像目标。在CoMM基准上使用Emu2模型，Pareto LoRA持续改善生成平衡，图像感知质量提升达44.9%，同时文本性能几乎未受影响。该工作为统一多模态模型的微调提供了有效的平衡策略，具有重要的理论和实践意义。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.17296v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1625, \"height\": 713, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17296v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 583, \"height\": 572, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17296v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 844, \"height\": 528, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17296v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 857, \"height\": 533, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17296v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 810, \"height\": 383, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17296v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 859, \"height\": 428, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17296v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 842, \"height\": 494, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.17296v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 867, \"height\": 491, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.17296v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1395, \"height\": 444, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17296v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1889, \"height\": 386, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17296v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1915, \"height\": 386, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17296v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1798, \"height\": 329, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17296v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1794, \"height\": 308, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.17296v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1786, \"height\": 343, \"label\": \"Table\"}]"
motivation: 现有统一多模态模型在LoRA微调中存在模态不平衡，语言梯度主导导致图像生成质量差，亟需平衡梯度的方法。
method: 提出Pareto LoRA，将多模态指令微调重构为双目标优化，通过帕累托最优梯度集成策略动态平衡文本与图像梯度。
result: "在CoMM基准上使用Emu2，图像感知质量提升44.9%，同时保持文本性能，验证了方法的有效性。"
conclusion: Pareto LoRA有效缓解了多模态模型微调中的模态不平衡，为统一多模态模型的参数高效微调提供了新思路。
---

## 摘要
统一多模态模型（UMMs）近期作为一种有前景的范式出现，能够在单个自回归Transformer中集成多模态理解与生成。然而，在多模态指令微调过程中，这些模型通常表现出显著的模式不平衡：语言梯度主导优化，从而导致图像生成质量较低，尤其是在使用LoRA等参数高效微调方法时。本文系统分析了基于LoRA的UMMs在交错文本-图像生成微调中的模态不平衡问题。我们发现，与单模态模型相比，视觉模态性能的下降幅度远大于文本模态，且不同任务和层中的模态特定梯度可能相差数个数量级。基于这一观察，我们将多模态指令微调重新表述为双目标优化问题，并提出Pareto LoRA，一种通过调节梯度方向和强度来平衡文本与图像目标的帕累托最优梯度集成策略。在CoMM基准上使用Emu2进行的实验表明，Pareto LoRA一致地改善了多模态生成平衡，在保持文本性能相当的同时，相比原始LoRA在感知图像质量上提升了高达44.9%。

## Abstract
Unified multimodal models (UMMs) have recently emerged as a promising paradigm for integrating multimodal understanding and generation within a single autoregressive transformer. However, during multimodal instruction tuning, these models often exhibit pronounced modality imbalance: language gradients dominate optimization, thus leading to lower image generation quality, especially under parameter-efficient fine-tuning such as LoRA. In this work, we systematically analyze modality imbalance in LoRA-based fine-tuning of UMMs for interleaved text-image generation. We show that vision modality performance degrades substantially more than text modality performance when compared to unimodal counterparts, and that modality-specific gradients can differ by orders of magnitude across various tasks and layers. Motivated by this observation, we reformulate the multimodal instruction tuning as a bi-objective optimization problem and propose Pareto LoRA, a Pareto-optimal gradient integration strategy that balances the text and image objectives by modulating the gradient direction and strength. Experiments on the CoMM benchmark with Emu2 demonstrate that Pareto LoRA consistently improves multimodal generation balance, achieving up to 44.9% gains in perceptual image quality over vanilla LoRA while maintaining comparable text performance.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：统一多模态模型（UMMs）在基于 LoRA 的参数高效指令微调中，存在严重的**模态不平衡**（modality imbalance）。语言（文本）梯度的幅度远超图像（视觉）梯度，主导优化过程，导致图像生成质量显著下降，而文本性能受影响较小。这种不平衡在不同任务、不同层中表现不一致，且传统的固定标量损失加权（如 `L = L_text + α L_img`）无法在梯度层面上有效缓解。
- **研究背景**：UMMs（如 Emu2）采用单一自回归 Transformer 骨干网络处理交错文本-图像生成；但 LoRA 微调中，文本模态更容易被优化，视觉模态被抑制，最终产生模糊、失真的图像或重复、空洞的文本输出。本文旨在通过**梯度级别的动态平衡**来解决这一根本性问题。

### 2. 方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：将多模态指令微调重新表述为**双目标优化问题**（bi-objective optimization），每个模态视为一个独立目标，通过寻找 Pareto 最优解来获得能够同时改善两个模态的梯度更新方向。
- **关键技术细节**：
  1. **梯度诊断**：在每一步训练中，计算文本和图像损失相对于 LoRA 参数的梯度 `g_text` 和 `g_img`，得到：
     - 余弦相似度 `c = (g_text · g_img) / (||g_text|| ||g_img||)` → 判断方向冲突
     - 梯度幅度比 `r = ||g_text|| / ||g_img||` → 判断幅度不平衡
  2. **三种情境处理**（图 5 和 Algorithm 1）：
     - **冲突情况**（c < 0）：梯度方向相反，通过 MGDA（Multiple Gradient Descent Algorithm）求解 min ||λ g_text + β g_img||，并约束 λ+β=1，得到 `λ = clip( ((g_text - g_img)^T g_text) / ||g_img - g_text||^2, 0, 1)`, β = 1-λ。最终梯度为加权组合。
     - **无冲突但幅度不平衡**（c ≥ 0 且 r > τ）：将较弱的梯度缩放至与较强梯度相同范数，再相加。τ 为预设阈值（默认 0.5）。
     - **无冲突且平衡**（c ≥ 0 且 r ≤ τ）：直接相加，不做修改。
  3. **层级分组调制**：观察到不同 LLM 层的梯度不平衡程度不同（图 6）。为避免逐层计算的昂贵开销，将 60 层 LLM 分为 5 组，默认仅对**低层组**（接近输入）应用调制，因为低层对视觉表示整合最为关键。
- **公式与算法文字说明**：主算法见 Algorithm 1，输入为 `L_text, L_img, τ`，输出为最终梯度 `g`，用于更新 LoRA 参数。

### 3. 实验设计

- **数据集**：CoMM（Coherent Interleaved Image-Text Dataset）包含四个任务：
  - Task 1：图像到文本生成
  - Task 2：文本到图像生成
  - Task 3：交错图像-文本内容生成
  - Task 4：基于问题的交错图像-文本生成
- **基准模型**：Emu2（37B 参数），仅对 LLM 骨干和投影层使用 LoRA（rank=32），视觉编码器和解码器冻结。
- **对比方法**：
  - Vanilla LoRA（无梯度调制）
  - GradNorm（基于梯度幅度重新加权损失）
  - Step Balance（基于训练步数的动态损失加权）
  - Zero Shot（无微调）
  - Unimodal Upper Bound（单独使用文本或图像目标微调的模型性能上限）
- **评估指标**：GPT-4o 自动评分（1–5 分），涵盖 Text Quality（文本质量）、Perceptual Quality（感知图像质量）、Image Coherence（图像一致性）、Text-Image Coherence（TIC）、Helpfulness（帮助性）五个维度。

### 4. 资源与算力

- **文中明确说明**：
  - 使用 8 块 **AMD Instinct™ MI300X GPU**，每 GPU batch size = 1。
  - 训练精度：BF16。
  - 学习率：1×10⁻⁵。
  - LoRA rank = 32。
- **未提及**：具体训练时长（如 epoch 数或步数）以及总参数量外的其他效率指标。

### 5. 实验数量与充分性

- **主要实验**：在 CoMM 四个任务上各报告了 5–6 个指标的结果（Tables 1–3），共包含 **12 组**（4 任务 × 3 对比方法 × 1 本文方法 + Zero Shot + Unimodal Upper Bound），数据完整。
- **消融实验**：
  - **调制目标层**（Table 4）：在 5 个层组（Lower / Middle-1,2,3 / Upper）上单独应用，说明最佳组为 Lower layer。
  - **调制组件**（Table 5）：分别测试仅使用冲突处理（Pareto-only）和仅使用幅度缩放（Rescale-only），证明两者结合（Full）最优。
  - **阈值 τ**（Table 6）：测试 τ = 0.5/0.6/0.7/0.8，确定 0.5 为最佳平衡点。
- **公平性**：对比了最常用的损失平衡方法（GradNorm 和 Step Balance），并设置了 Zero Shot 和 Unimodal Upper Bound 作为上下界。评估使用外部 judge（GPT-4o），降低了过拟合可能性。
- **总体评价**：实验设计较为全面，覆盖了不同任务类型和关键超参数。但仅在**单一模型（Emu2）和单一数据集（CoMM）** 上验证，泛化性仍有待进一步检验。

### 6. 论文的主要结论与发现

1. **LoRA 微调中模态不平衡普遍存在**：在四种 CoMM 任务中，三种出现文本梯度主导，视觉性能下降远大于文本。
2. **Pareto LoRA 效果显著**：相比 Vanilla LoRA，感知图像质量提升最高达 **44.9%**（Task 4），图像一致性提升最高 29.59%；文本质量基本持平或略有改善（Task 4 文本帮助性下降 9.29%，但图像指标大幅提升）。
3. **低层调制最佳**：接近输入的 LLM 低层组对视觉性能改善最为关键。
4. **同时处理冲突和幅度不平衡必要**：仅处理冲突（Pareto-only）或仅缩放幅度（Rescale-only）均不如完整方法。
5. **最佳阈值 τ = 0.5** 在总体平衡上表现最好。

### 7. 优点

- **方法简洁有效**：直接作用于梯度层面，无需修改模型架构或增加额外可训练参数。
- **自适应激活**：仅在检测到冲突或显著不平衡时才介入，保持了训练稳定性和效率。
- **层级感知的调制策略**：利用层间不平衡差异进行分组调制，兼顾效果与计算开销。
- **理论支撑扎实**：基于多目标优化中的 MGDA 和 Pareto 最优概念，具有坚实的数学基础。
- **消融实验充分**：清晰展示了各组件和超参数的作用。

### 8. 不足与局限

- **单模型单数据集的验证**：仅在 Emu2 + CoMM 上评测，未在 Show-o、Janus 等其他 UMMs 或更大规模数据集上验证，泛化能力存疑。
- **Task 4 中文本质量下降**：在语言主导的任务（问答式交错生成）中，Pareto LoRA 适度抑制了文本梯度，导致 Helpfulness 下降 9.29%，说明该方法对文本密集型任务可能引入“过度正则化”。
- **计算开销未量化**：虽然使用分组代替逐层调制，但未报告与 Vanilla LoRA 相比的训练时间或内存增加，实际部署效率不明。
- **阈值 τ 的敏感性**：虽然给出了实验选择 0.5，但缺乏理论指导如何在不同模型/任务中自动设定该阈值。
- **评价依赖 GPT-4o**：自动评分可能受模型偏见影响，且未提供人工评估作为补充验证。

（完）
